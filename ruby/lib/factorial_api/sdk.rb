# frozen_string_literal: true

# GENERATED by scripts/generate_sdk_layer.rb from the 2026-07-01 spec — DO NOT EDIT.
#
# Domain-namespaced ergonomic layer over the generated client:
#
#   api = F::Api.new(api_key: '...')
#   api.employees.employee.list(only_active: true, only_managers: false)
#   api.teams.team.get(id: 123)
#   api.timeoff.leave.paginate(max_items: 500).each { |leave| ... }
#
# Every call goes through the same shared ApiClient as the raw accessors
# (api.employees_employee.employees_employees_get), which keep working.

require 'factorial_api/api'

module F
  module Api
    # Emitted namespace/resource classes live in their own module so they
    # can never collide with the generated model classes under F::Api.
    module SDK
      # api.api_public.credential — wraps the raw api.api_public_credential.
      class ApiPublicCredentialResource
        def initialize(client)
          @raw = client.api_public_credential
        end

        # GET api_public/credentials
        def list(**opts)
          @raw.api_public_credentials_get(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.api_public.webhook_subscription — wraps the raw api.api_public_webhook_subscription.
      class ApiPublicWebhookSubscriptionResource
        def initialize(client)
          @raw = client.api_public_webhook_subscription
        end

        # GET api_public/webhook_subscriptions
        def list(**opts)
          @raw.api_public_webhook_subscriptions_get(opts)
        end

        # GET api_public/webhook_subscriptions/{id}
        def get(id:, **opts)
          @raw.api_public_webhook_subscriptions_id_get(id, opts)
        end

        # POST api_public/webhook_subscriptions
        def create(**opts)
          @raw.api_public_webhook_subscriptions_post(opts)
        end

        # PUT api_public/webhook_subscriptions/{id}
        def update(id:, **opts)
          @raw.api_public_webhook_subscriptions_id_put(id, opts)
        end

        # DELETE api_public/webhook_subscriptions/{id}
        def delete(id:, **opts)
          @raw.api_public_webhook_subscriptions_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.approvals.materialized_approvals_flow — wraps the raw api.approvals_materialized_approvals_flow.
      class ApprovalsMaterializedApprovalsFlowResource
        def initialize(client)
          @raw = client.approvals_materialized_approvals_flow
        end

        # POST approvals/materialized_approvals_flows/approve_resource
        def approve_resource(**opts)
          @raw.approvals_materialized_approvals_flows_approve_resource_post(opts)
        end

        # POST approvals/materialized_approvals_flows/reject_resource
        def reject_resource(**opts)
          @raw.approvals_materialized_approvals_flows_reject_resource_post(opts)
        end
      end

      # api.ats.answer — wraps the raw api.ats_answer.
      class AtsAnswerResource
        def initialize(client)
          @raw = client.ats_answer
        end

        # GET ats/answers
        def list(**opts)
          @raw.ats_answers_get(opts)
        end

        # GET ats/answers/{id}
        def get(id:, **opts)
          @raw.ats_answers_id_get(id, opts)
        end

        # POST ats/answers
        def create(**opts)
          @raw.ats_answers_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.application — wraps the raw api.ats_application.
      class AtsApplicationResource
        def initialize(client)
          @raw = client.ats_application
        end

        # GET ats/applications
        def list(**opts)
          @raw.ats_applications_get(opts)
        end

        # GET ats/applications/{id}
        def get(id:, **opts)
          @raw.ats_applications_id_get(id, opts)
        end

        # POST ats/applications
        def create(**opts)
          @raw.ats_applications_post(opts)
        end

        # PUT ats/applications/{id}
        def update(id:, **opts)
          @raw.ats_applications_id_put(id, opts)
        end

        # DELETE ats/applications/{id}
        def delete(id:, **opts)
          @raw.ats_applications_id_delete(id, opts)
        end

        # POST ats/applications/apply
        def apply(**opts)
          @raw.ats_applications_apply_post(opts)
        end

        # POST ats/applications/move_to_phase
        def move_to_phase(**opts)
          @raw.ats_applications_move_to_phase_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.application_phase — wraps the raw api.ats_application_phase.
      class AtsApplicationPhaseResource
        def initialize(client)
          @raw = client.ats_application_phase
        end

        # GET ats/application_phases
        def list(**opts)
          @raw.ats_application_phases_get(opts)
        end

        # GET ats/application_phases/{id}
        def get(id:, **opts)
          @raw.ats_application_phases_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.candidate — wraps the raw api.ats_candidate.
      class AtsCandidateResource
        def initialize(client)
          @raw = client.ats_candidate
        end

        # GET ats/candidates
        def list(**opts)
          @raw.ats_candidates_get(opts)
        end

        # GET ats/candidates/{id}
        def get(id:, **opts)
          @raw.ats_candidates_id_get(id, opts)
        end

        # POST ats/candidates
        def create(**opts)
          @raw.ats_candidates_post(opts)
        end

        # PUT ats/candidates/{id}
        def update(id:, **opts)
          @raw.ats_candidates_id_put(id, opts)
        end

        # DELETE ats/candidates/{id}
        def delete(id:, **opts)
          @raw.ats_candidates_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.candidate_source — wraps the raw api.ats_candidate_source.
      class AtsCandidateSourceResource
        def initialize(client)
          @raw = client.ats_candidate_source
        end

        # GET ats/candidate_sources
        def list(**opts)
          @raw.ats_candidate_sources_get(opts)
        end

        # GET ats/candidate_sources/{id}
        def get(id:, **opts)
          @raw.ats_candidate_sources_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.evaluation_form — wraps the raw api.ats_evaluation_form.
      class AtsEvaluationFormResource
        def initialize(client)
          @raw = client.ats_evaluation_form
        end

        # GET ats/evaluation_forms
        def list(**opts)
          @raw.ats_evaluation_forms_get(opts)
        end

        # GET ats/evaluation_forms/{id}
        def get(id:, **opts)
          @raw.ats_evaluation_forms_id_get(id, opts)
        end

        # POST ats/evaluation_forms/save_as_template
        def save_as_template(**opts)
          @raw.ats_evaluation_forms_save_as_template_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.feedback — wraps the raw api.ats_feedback.
      class AtsFeedbackResource
        def initialize(client)
          @raw = client.ats_feedback
        end

        # GET ats/feedbacks
        def list(**opts)
          @raw.ats_feedbacks_get(opts)
        end

        # GET ats/feedbacks/{id}
        def get(id:, **opts)
          @raw.ats_feedbacks_id_get(id, opts)
        end

        # POST ats/feedbacks
        def create(**opts)
          @raw.ats_feedbacks_post(opts)
        end

        # PUT ats/feedbacks/{id}
        def update(id:, **opts)
          @raw.ats_feedbacks_id_put(id, opts)
        end

        # DELETE ats/feedbacks/{id}
        def delete(id:, **opts)
          @raw.ats_feedbacks_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.hiring_stage — wraps the raw api.ats_hiring_stage.
      class AtsHiringStageResource
        def initialize(client)
          @raw = client.ats_hiring_stage
        end

        # GET ats/hiring_stages
        def list(**opts)
          @raw.ats_hiring_stages_get(opts)
        end

        # GET ats/hiring_stages/{id}
        def get(id:, **opts)
          @raw.ats_hiring_stages_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.job_posting — wraps the raw api.ats_job_posting.
      class AtsJobPostingResource
        def initialize(client)
          @raw = client.ats_job_posting
        end

        # GET ats/job_postings
        def list(**opts)
          @raw.ats_job_postings_get(opts)
        end

        # GET ats/job_postings/{id}
        def get(id:, **opts)
          @raw.ats_job_postings_id_get(id, opts)
        end

        # POST ats/job_postings
        def create(**opts)
          @raw.ats_job_postings_post(opts)
        end

        # PUT ats/job_postings/{id}
        def update(id:, **opts)
          @raw.ats_job_postings_id_put(id, opts)
        end

        # DELETE ats/job_postings/{id}
        def delete(id:, **opts)
          @raw.ats_job_postings_id_delete(id, opts)
        end

        # POST ats/job_postings/duplicate
        def duplicate(**opts)
          @raw.ats_job_postings_duplicate_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.message — wraps the raw api.ats_message.
      class AtsMessageResource
        def initialize(client)
          @raw = client.ats_message
        end

        # GET ats/messages
        def list(last_per_conversation:, **opts)
          @raw.ats_messages_get(last_per_conversation, opts)
        end

        # GET ats/messages/{id}
        def get(id:, **opts)
          @raw.ats_messages_id_get(id, opts)
        end

        # POST ats/messages
        def create(content:, sent_by_id:, sent_by_type:, ats_application_id:, attachments:, topic:, send_as_corporate_email:, **opts)
          @raw.ats_messages_post(content, sent_by_id, sent_by_type, ats_application_id, attachments, topic, send_as_corporate_email, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(last_per_conversation:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(last_per_conversation: last_per_conversation, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(last_per_conversation:, **opts)
          paginate(last_per_conversation: last_per_conversation, **opts).to_a
        end
      end

      # api.ats.question — wraps the raw api.ats_question.
      class AtsQuestionResource
        def initialize(client)
          @raw = client.ats_question
        end

        # GET ats/questions
        def list(**opts)
          @raw.ats_questions_get(opts)
        end

        # GET ats/questions/{id}
        def get(id:, **opts)
          @raw.ats_questions_id_get(id, opts)
        end

        # POST ats/questions
        def create(**opts)
          @raw.ats_questions_post(opts)
        end

        # PUT ats/questions/{id}
        def update(id:, **opts)
          @raw.ats_questions_id_put(id, opts)
        end

        # DELETE ats/questions/{id}
        def delete(id:, **opts)
          @raw.ats_questions_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.ats.rejection_reason — wraps the raw api.ats_rejection_reason.
      class AtsRejectionReasonResource
        def initialize(client)
          @raw = client.ats_rejection_reason
        end

        # GET ats/rejection_reasons
        def list(**opts)
          @raw.ats_rejection_reasons_get(opts)
        end

        # GET ats/rejection_reasons/{id}
        def get(id:, **opts)
          @raw.ats_rejection_reasons_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.attendance.break_configuration — wraps the raw api.attendance_break_configuration.
      class AttendanceBreakConfigurationResource
        def initialize(client)
          @raw = client.attendance_break_configuration
        end

        # GET attendance/break_configurations
        def list(**opts)
          @raw.attendance_break_configurations_get(opts)
        end

        # GET attendance/break_configurations/{id}
        def get(id:, **opts)
          @raw.attendance_break_configurations_id_get(id, opts)
        end

        # POST attendance/break_configurations
        def create(**opts)
          @raw.attendance_break_configurations_post(opts)
        end

        # PUT attendance/break_configurations/{id}
        def update(id:, **opts)
          @raw.attendance_break_configurations_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.attendance.edit_timesheet_request — wraps the raw api.attendance_edit_timesheet_request.
      class AttendanceEditTimesheetRequestResource
        def initialize(client)
          @raw = client.attendance_edit_timesheet_request
        end

        # GET attendance/edit_timesheet_requests
        def list(**opts)
          @raw.attendance_edit_timesheet_requests_get(opts)
        end

        # GET attendance/edit_timesheet_requests/{id}
        def get(id:, **opts)
          @raw.attendance_edit_timesheet_requests_id_get(id, opts)
        end

        # POST attendance/edit_timesheet_requests
        def create(**opts)
          @raw.attendance_edit_timesheet_requests_post(opts)
        end

        # PUT attendance/edit_timesheet_requests/{id}
        def update(id:, **opts)
          @raw.attendance_edit_timesheet_requests_id_put(id, opts)
        end

        # DELETE attendance/edit_timesheet_requests/{id}
        def delete(id:, **opts)
          @raw.attendance_edit_timesheet_requests_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.attendance.estimated_time — wraps the raw api.attendance_estimated_time.
      class AttendanceEstimatedTimeResource
        def initialize(client)
          @raw = client.attendance_estimated_time
        end

        # GET attendance/estimated_times
        def list(start_on:, end_on:, employee_ids:, **opts)
          @raw.attendance_estimated_times_get(start_on, end_on, employee_ids, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(start_on:, end_on:, employee_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(start_on: start_on, end_on: end_on, employee_ids: employee_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(start_on:, end_on:, employee_ids:, **opts)
          paginate(start_on: start_on, end_on: end_on, employee_ids: employee_ids, **opts).to_a
        end
      end

      # api.attendance.open_shift — wraps the raw api.attendance_open_shift.
      class AttendanceOpenShiftResource
        def initialize(client)
          @raw = client.attendance_open_shift
        end

        # GET attendance/open_shifts
        def list(**opts)
          @raw.attendance_open_shifts_get(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.attendance.overtime_request — wraps the raw api.attendance_overtime_request.
      class AttendanceOvertimeRequestResource
        def initialize(client)
          @raw = client.attendance_overtime_request
        end

        # GET attendance/overtime_requests
        def list(include_approval_flow:, **opts)
          @raw.attendance_overtime_requests_get(include_approval_flow, opts)
        end

        # GET attendance/overtime_requests/{id}
        def get(id:, **opts)
          @raw.attendance_overtime_requests_id_get(id, opts)
        end

        # POST attendance/overtime_requests
        def create(**opts)
          @raw.attendance_overtime_requests_post(opts)
        end

        # PUT attendance/overtime_requests/{id}
        def update(id:, **opts)
          @raw.attendance_overtime_requests_id_put(id, opts)
        end

        # DELETE attendance/overtime_requests/{id}
        def delete(id:, **opts)
          @raw.attendance_overtime_requests_id_delete(id, opts)
        end

        # POST attendance/overtime_requests/approve
        def approve(**opts)
          @raw.attendance_overtime_requests_approve_post(opts)
        end

        # POST attendance/overtime_requests/reject
        def reject(**opts)
          @raw.attendance_overtime_requests_reject_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_approval_flow:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_approval_flow: include_approval_flow, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_approval_flow:, **opts)
          paginate(include_approval_flow: include_approval_flow, **opts).to_a
        end
      end

      # api.attendance.review — wraps the raw api.attendance_review.
      class AttendanceReviewResource
        def initialize(client)
          @raw = client.attendance_review
        end

        # GET attendance/reviews
        def list(employee_ids:, start_on:, end_on:, reviewed_at:, **opts)
          @raw.attendance_reviews_get(employee_ids, start_on, end_on, reviewed_at, opts)
        end

        # POST attendance/reviews/bulk_create
        def bulk_create(**opts)
          @raw.attendance_reviews_bulk_create_post(opts)
        end

        # POST attendance/reviews/bulk_destroy
        def bulk_destroy(**opts)
          @raw.attendance_reviews_bulk_destroy_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(employee_ids:, start_on:, end_on:, reviewed_at:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(employee_ids: employee_ids, start_on: start_on, end_on: end_on, reviewed_at: reviewed_at, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(employee_ids:, start_on:, end_on:, reviewed_at:, **opts)
          paginate(employee_ids: employee_ids, start_on: start_on, end_on: end_on, reviewed_at: reviewed_at, **opts).to_a
        end
      end

      # api.attendance.shift — wraps the raw api.attendance_shift.
      class AttendanceShiftResource
        def initialize(client)
          @raw = client.attendance_shift
        end

        # GET attendance/shifts
        def list(half_day:, sort_created_at_asc:, **opts)
          @raw.attendance_shifts_get(half_day, sort_created_at_asc, opts)
        end

        # GET attendance/shifts/{id}
        def get(id:, **opts)
          @raw.attendance_shifts_id_get(id, opts)
        end

        # POST attendance/shifts
        def create(**opts)
          @raw.attendance_shifts_post(opts)
        end

        # PUT attendance/shifts/{id}
        def update(id:, **opts)
          @raw.attendance_shifts_id_put(id, opts)
        end

        # DELETE attendance/shifts/{id}
        def delete(id:, **opts)
          @raw.attendance_shifts_id_delete(id, opts)
        end

        # POST attendance/shifts/autofill
        def autofill(**opts)
          @raw.attendance_shifts_autofill_post(opts)
        end

        # POST attendance/shifts/break_end
        def break_end(**opts)
          @raw.attendance_shifts_break_end_post(opts)
        end

        # POST attendance/shifts/break_start
        def break_start(**opts)
          @raw.attendance_shifts_break_start_post(opts)
        end

        # POST attendance/shifts/clock_in
        def clock_in(**opts)
          @raw.attendance_shifts_clock_in_post(opts)
        end

        # POST attendance/shifts/clock_out
        def clock_out(**opts)
          @raw.attendance_shifts_clock_out_post(opts)
        end

        # POST attendance/shifts/toggle_clock
        def toggle_clock(**opts)
          @raw.attendance_shifts_toggle_clock_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(half_day:, sort_created_at_asc:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(half_day: half_day, sort_created_at_asc: sort_created_at_asc, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(half_day:, sort_created_at_asc:, **opts)
          paginate(half_day: half_day, sort_created_at_asc: sort_created_at_asc, **opts).to_a
        end
      end

      # api.attendance.worked_time — wraps the raw api.attendance_worked_time.
      class AttendanceWorkedTimeResource
        def initialize(client)
          @raw = client.attendance_worked_time
        end

        # GET attendance/worked_times
        def list(include_time_range_category:, include_non_attendable_employees:, **opts)
          @raw.attendance_worked_times_get(include_time_range_category, include_non_attendable_employees, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_time_range_category:, include_non_attendable_employees:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_time_range_category: include_time_range_category, include_non_attendable_employees: include_non_attendable_employees, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_time_range_category:, include_non_attendable_employees:, **opts)
          paginate(include_time_range_category: include_time_range_category, include_non_attendable_employees: include_non_attendable_employees, **opts).to_a
        end
      end

      # api.banking.bank_account — wraps the raw api.banking_bank_account.
      class BankingBankAccountResource
        def initialize(client)
          @raw = client.banking_bank_account
        end

        # GET banking/bank_accounts
        def list(**opts)
          @raw.banking_bank_accounts_get(opts)
        end

        # GET banking/bank_accounts/{id}
        def get(id:, **opts)
          @raw.banking_bank_accounts_id_get(id, opts)
        end

        # POST banking/bank_accounts/create_manual
        def create_manual(**opts)
          @raw.banking_bank_accounts_create_manual_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.banking.card_payment — wraps the raw api.banking_card_payment.
      class BankingCardPaymentResource
        def initialize(client)
          @raw = client.banking_card_payment
        end

        # GET banking/card_payments
        def list(**opts)
          @raw.banking_card_payments_get(opts)
        end

        # GET banking/card_payments/{id}
        def get(id:, **opts)
          @raw.banking_card_payments_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.banking.transaction — wraps the raw api.banking_transaction.
      class BankingTransactionResource
        def initialize(client)
          @raw = client.banking_transaction
        end

        # GET banking/transactions
        def list(**opts)
          @raw.banking_transactions_get(opts)
        end

        # GET banking/transactions/{id}
        def get(id:, **opts)
          @raw.banking_transactions_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.bookkeepers_management.incidence — wraps the raw api.bookkeepers_management_incidence.
      class BookkeepersManagementIncidenceResource
        def initialize(client)
          @raw = client.bookkeepers_management_incidence
        end

        # GET bookkeepers_management/incidences
        def list(**opts)
          @raw.bookkeepers_management_incidences_get(opts)
        end

        # GET bookkeepers_management/incidences/{id}
        def get(id:, **opts)
          @raw.bookkeepers_management_incidences_id_get(id, opts)
        end

        # PUT bookkeepers_management/incidences/{id}
        def update(id:, **opts)
          @raw.bookkeepers_management_incidences_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.companies.legal_entity — wraps the raw api.companies_legal_entity.
      class CompaniesLegalEntityResource
        def initialize(client)
          @raw = client.companies_legal_entity
        end

        # GET companies/legal_entities
        def list(**opts)
          @raw.companies_legal_entities_get(opts)
        end

        # GET companies/legal_entities/{id}
        def get(id:, **opts)
          @raw.companies_legal_entities_id_get(id, opts)
        end

        # POST companies/legal_entities
        def create(**opts)
          @raw.companies_legal_entities_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.compensations.concept — wraps the raw api.compensations_concept.
      class CompensationsConceptResource
        def initialize(client)
          @raw = client.compensations_concept
        end

        # GET compensations/concepts
        def list(**opts)
          @raw.compensations_concepts_get(opts)
        end

        # GET compensations/concepts/{id}
        def get(id:, **opts)
          @raw.compensations_concepts_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.compensation — wraps the raw api.contracts_compensation.
      class ContractsCompensationResource
        def initialize(client)
          @raw = client.contracts_compensation
        end

        # GET contracts/compensations
        def list(**opts)
          @raw.contracts_compensations_get(opts)
        end

        # GET contracts/compensations/{id}
        def get(id:, **opts)
          @raw.contracts_compensations_id_get(id, opts)
        end

        # POST contracts/compensations
        def create(**opts)
          @raw.contracts_compensations_post(opts)
        end

        # PUT contracts/compensations/{id}
        def update(id:, **opts)
          @raw.contracts_compensations_id_put(id, opts)
        end

        # DELETE contracts/compensations/{id}
        def delete(id:, **opts)
          @raw.contracts_compensations_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.contract_template — wraps the raw api.contracts_contract_template.
      class ContractsContractTemplateResource
        def initialize(client)
          @raw = client.contracts_contract_template
        end

        # GET contracts/contract_templates
        def list(**opts)
          @raw.contracts_contract_templates_get(opts)
        end

        # GET contracts/contract_templates/{id}
        def get(id:, **opts)
          @raw.contracts_contract_templates_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.contract_version — wraps the raw api.contracts_contract_version.
      class ContractsContractVersionResource
        def initialize(client)
          @raw = client.contracts_contract_version
        end

        # GET contracts/contract_versions
        def list(job_catalog_tree_node_uuids:, **opts)
          @raw.contracts_contract_versions_get(job_catalog_tree_node_uuids, opts)
        end

        # GET contracts/contract_versions/{id}
        def get(id:, **opts)
          @raw.contracts_contract_versions_id_get(id, opts)
        end

        # POST contracts/contract_versions
        def create(**opts)
          @raw.contracts_contract_versions_post(opts)
        end

        # PUT contracts/contract_versions/{id}
        def update(id:, **opts)
          @raw.contracts_contract_versions_id_put(id, opts)
        end

        # DELETE contracts/contract_versions/{id}
        def delete(id:, **opts)
          @raw.contracts_contract_versions_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(job_catalog_tree_node_uuids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(job_catalog_tree_node_uuids: job_catalog_tree_node_uuids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(job_catalog_tree_node_uuids:, **opts)
          paginate(job_catalog_tree_node_uuids: job_catalog_tree_node_uuids, **opts).to_a
        end
      end

      # api.contracts.contract_version_history — wraps the raw api.contracts_contract_version_history.
      class ContractsContractVersionHistoryResource
        def initialize(client)
          @raw = client.contracts_contract_version_history
        end

        # GET contracts/contract_version_histories
        def list(**opts)
          @raw.contracts_contract_version_histories_get(opts)
        end

        # GET contracts/contract_version_histories/{id}
        def get(id:, **opts)
          @raw.contracts_contract_version_histories_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.contract_version_meta_datum — wraps the raw api.contracts_contract_version_meta_datum.
      class ContractsContractVersionMetaDatumResource
        def initialize(client)
          @raw = client.contracts_contract_version_meta_datum
        end

        # GET contracts/contract_version_meta_data
        def list(contract_version_ids:, **opts)
          @raw.contracts_contract_version_meta_data_get(contract_version_ids, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(contract_version_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(contract_version_ids: contract_version_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(contract_version_ids:, **opts)
          paginate(contract_version_ids: contract_version_ids, **opts).to_a
        end
      end

      # api.contracts.french_contract_type — wraps the raw api.contracts_french_contract_type.
      class ContractsFrenchContractTypeResource
        def initialize(client)
          @raw = client.contracts_french_contract_type
        end

        # GET contracts/french_contract_types
        def list(**opts)
          @raw.contracts_french_contract_types_get(opts)
        end

        # GET contracts/french_contract_types/{id}
        def get(id:, **opts)
          @raw.contracts_french_contract_types_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.german_contract_type — wraps the raw api.contracts_german_contract_type.
      class ContractsGermanContractTypeResource
        def initialize(client)
          @raw = client.contracts_german_contract_type
        end

        # GET contracts/german_contract_types
        def list(**opts)
          @raw.contracts_german_contract_types_get(opts)
        end

        # GET contracts/german_contract_types/{id}
        def get(id:, **opts)
          @raw.contracts_german_contract_types_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.materialized_template — wraps the raw api.contracts_materialized_template.
      class ContractsMaterializedTemplateResource
        def initialize(client)
          @raw = client.contracts_materialized_template
        end

        # GET contracts/materialized_templates
        def list(company_id:, template_type:, include_archived:, **opts)
          @raw.contracts_materialized_templates_get(company_id, template_type, include_archived, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(company_id:, template_type:, include_archived:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(company_id: company_id, template_type: template_type, include_archived: include_archived, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(company_id:, template_type:, include_archived:, **opts)
          paginate(company_id: company_id, template_type: template_type, include_archived: include_archived, **opts).to_a
        end
      end

      # api.contracts.portuguese_contract_type — wraps the raw api.contracts_portuguese_contract_type.
      class ContractsPortugueseContractTypeResource
        def initialize(client)
          @raw = client.contracts_portuguese_contract_type
        end

        # GET contracts/portuguese_contract_types
        def list(**opts)
          @raw.contracts_portuguese_contract_types_get(opts)
        end

        # GET contracts/portuguese_contract_types/{id}
        def get(id:, **opts)
          @raw.contracts_portuguese_contract_types_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.reference_contract — wraps the raw api.contracts_reference_contract.
      class ContractsReferenceContractResource
        def initialize(client)
          @raw = client.contracts_reference_contract
        end

        # GET contracts/reference_contracts
        def list(job_catalog_tree_node_uuids:, **opts)
          @raw.contracts_reference_contracts_get(job_catalog_tree_node_uuids, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(job_catalog_tree_node_uuids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(job_catalog_tree_node_uuids: job_catalog_tree_node_uuids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(job_catalog_tree_node_uuids:, **opts)
          paginate(job_catalog_tree_node_uuids: job_catalog_tree_node_uuids, **opts).to_a
        end
      end

      # api.contracts.spanish_contract_type — wraps the raw api.contracts_spanish_contract_type.
      class ContractsSpanishContractTypeResource
        def initialize(client)
          @raw = client.contracts_spanish_contract_type
        end

        # GET contracts/spanish_contract_types
        def list(**opts)
          @raw.contracts_spanish_contract_types_get(opts)
        end

        # GET contracts/spanish_contract_types/{id}
        def get(id:, **opts)
          @raw.contracts_spanish_contract_types_id_get(id, opts)
        end

        # POST contracts/spanish_contract_types
        def create(**opts)
          @raw.contracts_spanish_contract_types_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.spanish_education_level — wraps the raw api.contracts_spanish_education_level.
      class ContractsSpanishEducationLevelResource
        def initialize(client)
          @raw = client.contracts_spanish_education_level
        end

        # GET contracts/spanish_education_levels
        def list(**opts)
          @raw.contracts_spanish_education_levels_get(opts)
        end

        # GET contracts/spanish_education_levels/{id}
        def get(id:, **opts)
          @raw.contracts_spanish_education_levels_id_get(id, opts)
        end

        # POST contracts/spanish_education_levels
        def create(**opts)
          @raw.contracts_spanish_education_levels_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.spanish_professional_category — wraps the raw api.contracts_spanish_professional_category.
      class ContractsSpanishProfessionalCategoryResource
        def initialize(client)
          @raw = client.contracts_spanish_professional_category
        end

        # GET contracts/spanish_professional_categories
        def list(**opts)
          @raw.contracts_spanish_professional_categories_get(opts)
        end

        # GET contracts/spanish_professional_categories/{id}
        def get(id:, **opts)
          @raw.contracts_spanish_professional_categories_id_get(id, opts)
        end

        # POST contracts/spanish_professional_categories
        def create(**opts)
          @raw.contracts_spanish_professional_categories_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.spanish_working_day_type — wraps the raw api.contracts_spanish_working_day_type.
      class ContractsSpanishWorkingDayTypeResource
        def initialize(client)
          @raw = client.contracts_spanish_working_day_type
        end

        # GET contracts/spanish_working_day_types
        def list(**opts)
          @raw.contracts_spanish_working_day_types_get(opts)
        end

        # GET contracts/spanish_working_day_types/{id}
        def get(id:, **opts)
          @raw.contracts_spanish_working_day_types_id_get(id, opts)
        end

        # POST contracts/spanish_working_day_types
        def create(**opts)
          @raw.contracts_spanish_working_day_types_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.contracts.taxonomy — wraps the raw api.contracts_taxonomy.
      class ContractsTaxonomyResource
        def initialize(client)
          @raw = client.contracts_taxonomy
        end

        # GET contracts/taxonomies
        def list(**opts)
          @raw.contracts_taxonomies_get(opts)
        end

        # GET contracts/taxonomies/{id}
        def get(id:, **opts)
          @raw.contracts_taxonomies_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_fields.field — wraps the raw api.custom_fields_field.
      class CustomFieldsFieldResource
        def initialize(client)
          @raw = client.custom_fields_field
        end

        # GET custom_fields/fields
        def list(**opts)
          @raw.custom_fields_fields_get(opts)
        end

        # GET custom_fields/fields/{id}
        def get(id:, **opts)
          @raw.custom_fields_fields_id_get(id, opts)
        end

        # POST custom_fields/fields
        def create(**opts)
          @raw.custom_fields_fields_post(opts)
        end

        # DELETE custom_fields/fields/{id}
        def delete(id:, **opts)
          @raw.custom_fields_fields_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_fields.option — wraps the raw api.custom_fields_option.
      class CustomFieldsOptionResource
        def initialize(client)
          @raw = client.custom_fields_option
        end

        # GET custom_fields/options
        def list(**opts)
          @raw.custom_fields_options_get(opts)
        end

        # GET custom_fields/options/{id}
        def get(id:, **opts)
          @raw.custom_fields_options_id_get(id, opts)
        end

        # POST custom_fields/options
        def create(**opts)
          @raw.custom_fields_options_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_fields.resource_field — wraps the raw api.custom_fields_resource_field.
      class CustomFieldsResourceFieldResource
        def initialize(client)
          @raw = client.custom_fields_resource_field
        end

        # GET custom_fields/resource_fields
        def list(**opts)
          @raw.custom_fields_resource_fields_get(opts)
        end

        # GET custom_fields/resource_fields/{id}
        def get(id:, **opts)
          @raw.custom_fields_resource_fields_id_get(id, opts)
        end

        # POST custom_fields/resource_fields
        def create(**opts)
          @raw.custom_fields_resource_fields_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_fields.value — wraps the raw api.custom_fields_value.
      class CustomFieldsValueResource
        def initialize(client)
          @raw = client.custom_fields_value
        end

        # GET custom_fields/values
        def list(**opts)
          @raw.custom_fields_values_get(opts)
        end

        # GET custom_fields/values/{id}
        def get(id:, **opts)
          @raw.custom_fields_values_id_get(id, opts)
        end

        # POST custom_fields/values
        def create(**opts)
          @raw.custom_fields_values_post(opts)
        end

        # PUT custom_fields/values/{id}
        def update(id:, **opts)
          @raw.custom_fields_values_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_resources.resource — wraps the raw api.custom_resources_resource.
      class CustomResourcesResourceResource
        def initialize(client)
          @raw = client.custom_resources_resource
        end

        # GET custom_resources/resources
        def list(**opts)
          @raw.custom_resources_resources_get(opts)
        end

        # GET custom_resources/resources/{id}
        def get(id:, **opts)
          @raw.custom_resources_resources_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_resources.schema — wraps the raw api.custom_resources_schema.
      class CustomResourcesSchemaResource
        def initialize(client)
          @raw = client.custom_resources_schema
        end

        # GET custom_resources/schemas
        def list(**opts)
          @raw.custom_resources_schemas_get(opts)
        end

        # GET custom_resources/schemas/{id}
        def get(id:, **opts)
          @raw.custom_resources_schemas_id_get(id, opts)
        end

        # POST custom_resources/schemas
        def create(**opts)
          @raw.custom_resources_schemas_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.custom_resources.value — wraps the raw api.custom_resources_value.
      class CustomResourcesValueResource
        def initialize(client)
          @raw = client.custom_resources_value
        end

        # GET custom_resources/values
        def list(**opts)
          @raw.custom_resources_values_get(opts)
        end

        # GET custom_resources/values/{id}
        def get(id:, **opts)
          @raw.custom_resources_values_id_get(id, opts)
        end

        # POST custom_resources/values
        def create(**opts)
          @raw.custom_resources_values_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.documents.document — wraps the raw api.documents_document.
      class DocumentsDocumentResource
        def initialize(client)
          @raw = client.documents_document
        end

        # GET documents/documents
        def list(by_pending_assignment:, by_trash_bin:, **opts)
          @raw.documents_documents_get(by_pending_assignment, by_trash_bin, opts)
        end

        # GET documents/documents/{id}
        def get(id:, **opts)
          @raw.documents_documents_id_get(id, opts)
        end

        # POST documents/documents
        def create(public:, space:, is_pending_assignment:, file:, author_id:, company_id:, signee_ids:, request_esignature:, **opts)
          @raw.documents_documents_post(public, space, is_pending_assignment, file, author_id, company_id, signee_ids, request_esignature, opts)
        end

        # PUT documents/documents/{id}
        def update(id:, **opts)
          @raw.documents_documents_id_put(id, opts)
        end

        # DELETE documents/documents/{id}
        def delete(id:, **opts)
          @raw.documents_documents_id_delete(id, opts)
        end

        # POST documents/documents/move_to_trash_bin
        def move_to_trash_bin(**opts)
          @raw.documents_documents_move_to_trash_bin_post(opts)
        end

        # POST documents/documents/restore_from_trash_bin
        def restore_from_trash_bin(**opts)
          @raw.documents_documents_restore_from_trash_bin_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(by_pending_assignment:, by_trash_bin:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(by_pending_assignment: by_pending_assignment, by_trash_bin: by_trash_bin, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(by_pending_assignment:, by_trash_bin:, **opts)
          paginate(by_pending_assignment: by_pending_assignment, by_trash_bin: by_trash_bin, **opts).to_a
        end
      end

      # api.documents.download_url — wraps the raw api.documents_download_url.
      class DocumentsDownloadUrlResource
        def initialize(client)
          @raw = client.documents_download_url
        end

        # POST documents/download_urls/bulk_create
        def bulk_create(**opts)
          @raw.documents_download_urls_bulk_create_post(opts)
        end
      end

      # api.documents.folder — wraps the raw api.documents_folder.
      class DocumentsFolderResource
        def initialize(client)
          @raw = client.documents_folder
        end

        # GET documents/folders
        def list(**opts)
          @raw.documents_folders_get(opts)
        end

        # GET documents/folders/{id}
        def get(id:, **opts)
          @raw.documents_folders_id_get(id, opts)
        end

        # POST documents/folders
        def create(**opts)
          @raw.documents_folders_post(opts)
        end

        # PUT documents/folders/{id}
        def update(id:, **opts)
          @raw.documents_folders_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employee_updates.absence — wraps the raw api.employee_updates_absence.
      class EmployeeUpdatesAbsenceResource
        def initialize(client)
          @raw = client.employee_updates_absence
        end

        # GET employee_updates/absences
        def list(**opts)
          @raw.employee_updates_absences_get(opts)
        end

        # GET employee_updates/absences/{id}
        def get(id:, **opts)
          @raw.employee_updates_absences_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employee_updates.contract_change — wraps the raw api.employee_updates_contract_change.
      class EmployeeUpdatesContractChangeResource
        def initialize(client)
          @raw = client.employee_updates_contract_change
        end

        # GET employee_updates/contract_changes
        def list(**opts)
          @raw.employee_updates_contract_changes_get(opts)
        end

        # GET employee_updates/contract_changes/{id}
        def get(id:, **opts)
          @raw.employee_updates_contract_changes_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employee_updates.new_hire — wraps the raw api.employee_updates_new_hire.
      class EmployeeUpdatesNewHireResource
        def initialize(client)
          @raw = client.employee_updates_new_hire
        end

        # GET employee_updates/new_hires
        def list(**opts)
          @raw.employee_updates_new_hires_get(opts)
        end

        # GET employee_updates/new_hires/{id}
        def get(id:, **opts)
          @raw.employee_updates_new_hires_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employee_updates.personal_change — wraps the raw api.employee_updates_personal_change.
      class EmployeeUpdatesPersonalChangeResource
        def initialize(client)
          @raw = client.employee_updates_personal_change
        end

        # GET employee_updates/personal_changes
        def list(**opts)
          @raw.employee_updates_personal_changes_get(opts)
        end

        # GET employee_updates/personal_changes/{id}
        def get(id:, **opts)
          @raw.employee_updates_personal_changes_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employee_updates.summary — wraps the raw api.employee_updates_summary.
      class EmployeeUpdatesSummaryResource
        def initialize(client)
          @raw = client.employee_updates_summary
        end

        # GET employee_updates/summaries
        def list(**opts)
          @raw.employee_updates_summaries_get(opts)
        end

        # GET employee_updates/summaries/{id}
        def get(id:, **opts)
          @raw.employee_updates_summaries_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employee_updates.termination — wraps the raw api.employee_updates_termination.
      class EmployeeUpdatesTerminationResource
        def initialize(client)
          @raw = client.employee_updates_termination
        end

        # GET employee_updates/terminations
        def list(**opts)
          @raw.employee_updates_terminations_get(opts)
        end

        # GET employee_updates/terminations/{id}
        def get(id:, **opts)
          @raw.employee_updates_terminations_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.employees.employee — wraps the raw api.employees_employee.
      class EmployeesEmployeeResource
        def initialize(client)
          @raw = client.employees_employee
        end

        # GET employees/employees
        def list(only_active:, only_managers:, **opts)
          @raw.employees_employees_get(only_active, only_managers, opts)
        end

        # GET employees/employees/{id}
        def get(id:, **opts)
          @raw.employees_employees_id_get(id, opts)
        end

        # PUT employees/employees/{id}
        def update(id:, **opts)
          @raw.employees_employees_id_put(id, opts)
        end

        # POST employees/employees/create_with_contract
        def create_with_contract(**opts)
          @raw.employees_employees_create_with_contract_post(opts)
        end

        # POST employees/employees/invite
        def invite(**opts)
          @raw.employees_employees_invite_post(opts)
        end

        # POST employees/employees/set_regular_access_start_date
        def set_regular_access_start_date(**opts)
          @raw.employees_employees_set_regular_access_start_date_post(opts)
        end

        # POST employees/employees/terminate
        def terminate(**opts)
          @raw.employees_employees_terminate_post(opts)
        end

        # POST employees/employees/unterminate
        def unterminate(**opts)
          @raw.employees_employees_unterminate_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(only_active:, only_managers:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(only_active: only_active, only_managers: only_managers, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(only_active:, only_managers:, **opts)
          paginate(only_active: only_active, only_managers: only_managers, **opts).to_a
        end
      end

      # api.expenses.expensable — wraps the raw api.expenses_expensable.
      class ExpensesExpensableResource
        def initialize(client)
          @raw = client.expenses_expensable
        end

        # GET expenses/expensables
        def list(include_grouped:, include_attachments:, include_manual_drafts:, **opts)
          @raw.expenses_expensables_get(include_grouped, include_attachments, include_manual_drafts, opts)
        end

        # GET expenses/expensables/{id}
        def get(id:, **opts)
          @raw.expenses_expensables_id_get(id, opts)
        end

        # POST expenses/expensables/bulk_set_to_paid
        def bulk_set_to_paid(**opts)
          @raw.expenses_expensables_bulk_set_to_paid_post(opts)
        end

        # POST expenses/expensables/update_reimbursable_amount
        def update_reimbursable_amount(**opts)
          @raw.expenses_expensables_update_reimbursable_amount_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_grouped:, include_attachments:, include_manual_drafts:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_grouped: include_grouped, include_attachments: include_attachments, include_manual_drafts: include_manual_drafts, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_grouped:, include_attachments:, include_manual_drafts:, **opts)
          paginate(include_grouped: include_grouped, include_attachments: include_attachments, include_manual_drafts: include_manual_drafts, **opts).to_a
        end
      end

      # api.expenses.expense — wraps the raw api.expenses_expense.
      class ExpensesExpenseResource
        def initialize(client)
          @raw = client.expenses_expense
        end

        # GET expenses/expenses
        def list(include_manual_drafts:, include_attachments:, **opts)
          @raw.expenses_expenses_get(include_manual_drafts, include_attachments, opts)
        end

        # GET expenses/expenses/{id}
        def get(id:, **opts)
          @raw.expenses_expenses_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_manual_drafts:, include_attachments:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_manual_drafts: include_manual_drafts, include_attachments: include_attachments, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_manual_drafts:, include_attachments:, **opts)
          paginate(include_manual_drafts: include_manual_drafts, include_attachments: include_attachments, **opts).to_a
        end
      end

      # api.expenses.mileage — wraps the raw api.expenses_mileage.
      class ExpensesMileageResource
        def initialize(client)
          @raw = client.expenses_mileage
        end

        # GET expenses/mileages
        def list(include_manual_drafts:, include_attachments:, **opts)
          @raw.expenses_mileages_get(include_manual_drafts, include_attachments, opts)
        end

        # GET expenses/mileages/{id}
        def get(id:, **opts)
          @raw.expenses_mileages_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_manual_drafts:, include_attachments:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_manual_drafts: include_manual_drafts, include_attachments: include_attachments, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_manual_drafts:, include_attachments:, **opts)
          paginate(include_manual_drafts: include_manual_drafts, include_attachments: include_attachments, **opts).to_a
        end
      end

      # api.expenses.per_diem — wraps the raw api.expenses_per_diem.
      class ExpensesPerDiemResource
        def initialize(client)
          @raw = client.expenses_per_diem
        end

        # GET expenses/per_diems
        def list(**opts)
          @raw.expenses_per_diems_get(opts)
        end

        # GET expenses/per_diems/{id}
        def get(id:, **opts)
          @raw.expenses_per_diems_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.account — wraps the raw api.finance_account.
      class FinanceAccountResource
        def initialize(client)
          @raw = client.finance_account
        end

        # GET finance/accounts
        def list(**opts)
          @raw.finance_accounts_get(opts)
        end

        # GET finance/accounts/{id}
        def get(id:, **opts)
          @raw.finance_accounts_id_get(id, opts)
        end

        # POST finance/accounts
        def create(**opts)
          @raw.finance_accounts_post(opts)
        end

        # PUT finance/accounts/{id}
        def update(id:, **opts)
          @raw.finance_accounts_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.accounting_setting — wraps the raw api.finance_accounting_setting.
      class FinanceAccountingSettingResource
        def initialize(client)
          @raw = client.finance_accounting_setting
        end

        # GET finance/accounting_settings
        def list(**opts)
          @raw.finance_accounting_settings_get(opts)
        end

        # GET finance/accounting_settings/{id}
        def get(id:, **opts)
          @raw.finance_accounting_settings_id_get(id, opts)
        end

        # POST finance/accounting_settings/upsert
        def upsert(**opts)
          @raw.finance_accounting_settings_upsert_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.budget_option — wraps the raw api.finance_budget_option.
      class FinanceBudgetOptionResource
        def initialize(client)
          @raw = client.finance_budget_option
        end

        # GET finance/budget_options
        def list(include_inactive:, include_archived:, **opts)
          @raw.finance_budget_options_get(include_inactive, include_archived, opts)
        end

        # GET finance/budget_options/{id}
        def get(id:, **opts)
          @raw.finance_budget_options_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_inactive:, include_archived:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_inactive: include_inactive, include_archived: include_archived, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_inactive:, include_archived:, **opts)
          paginate(include_inactive: include_inactive, include_archived: include_archived, **opts).to_a
        end
      end

      # api.finance.category — wraps the raw api.finance_category.
      class FinanceCategoryResource
        def initialize(client)
          @raw = client.finance_category
        end

        # GET finance/categories
        def list(category_level:, type:, statuses:, **opts)
          @raw.finance_categories_get(category_level, type, statuses, opts)
        end

        # GET finance/categories/{id}
        def get(id:, **opts)
          @raw.finance_categories_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(category_level:, type:, statuses:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(category_level: category_level, type: type, statuses: statuses, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(category_level:, type:, statuses:, **opts)
          paginate(category_level: category_level, type: type, statuses: statuses, **opts).to_a
        end
      end

      # api.finance.contact — wraps the raw api.finance_contact.
      class FinanceContactResource
        def initialize(client)
          @raw = client.finance_contact
        end

        # GET finance/contacts
        def list(**opts)
          @raw.finance_contacts_get(opts)
        end

        # GET finance/contacts/{id}
        def get(id:, **opts)
          @raw.finance_contacts_id_get(id, opts)
        end

        # POST finance/contacts
        def create(**opts)
          @raw.finance_contacts_post(opts)
        end

        # PUT finance/contacts/{id}
        def update(id:, **opts)
          @raw.finance_contacts_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.cost_center — wraps the raw api.finance_cost_center.
      class FinanceCostCenterResource
        def initialize(client)
          @raw = client.finance_cost_center
        end

        # GET finance/cost_centers
        def list(**opts)
          @raw.finance_cost_centers_get(opts)
        end

        # GET finance/cost_centers/{id}
        def get(id:, **opts)
          @raw.finance_cost_centers_id_get(id, opts)
        end

        # POST finance/cost_centers
        def create(**opts)
          @raw.finance_cost_centers_post(opts)
        end

        # DELETE finance/cost_centers/{id}
        def delete(id:, **opts)
          @raw.finance_cost_centers_id_delete(id, opts)
        end

        # POST finance/cost_centers/edit
        def edit(**opts)
          @raw.finance_cost_centers_edit_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.cost_center_membership — wraps the raw api.finance_cost_center_membership.
      class FinanceCostCenterMembershipResource
        def initialize(client)
          @raw = client.finance_cost_center_membership
        end

        # GET finance/cost_center_memberships
        def list(**opts)
          @raw.finance_cost_center_memberships_get(opts)
        end

        # POST finance/cost_center_memberships/bulk_create_update
        def bulk_create_update(**opts)
          @raw.finance_cost_center_memberships_bulk_create_update_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.financial_document — wraps the raw api.finance_financial_document.
      class FinanceFinancialDocumentResource
        def initialize(client)
          @raw = client.finance_financial_document
        end

        # GET finance/financial_documents
        def list(**opts)
          @raw.finance_financial_documents_get(opts)
        end

        # GET finance/financial_documents/{id}
        def get(id:, **opts)
          @raw.finance_financial_documents_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.journal_entry — wraps the raw api.finance_journal_entry.
      class FinanceJournalEntryResource
        def initialize(client)
          @raw = client.finance_journal_entry
        end

        # GET finance/journal_entries
        def list(**opts)
          @raw.finance_journal_entries_get(opts)
        end

        # GET finance/journal_entries/{id}
        def get(id:, **opts)
          @raw.finance_journal_entries_id_get(id, opts)
        end

        # POST finance/journal_entries
        def create(**opts)
          @raw.finance_journal_entries_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.journal_line — wraps the raw api.finance_journal_line.
      class FinanceJournalLineResource
        def initialize(client)
          @raw = client.finance_journal_line
        end

        # GET finance/journal_lines
        def list(**opts)
          @raw.finance_journal_lines_get(opts)
        end

        # GET finance/journal_lines/{id}
        def get(id:, **opts)
          @raw.finance_journal_lines_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.ledger_account_resource — wraps the raw api.finance_ledger_account_resource.
      class FinanceLedgerAccountResourceResource
        def initialize(client)
          @raw = client.finance_ledger_account_resource
        end

        # GET finance/ledger_account_resources
        def list(**opts)
          @raw.finance_ledger_account_resources_get(opts)
        end

        # GET finance/ledger_account_resources/{id}
        def get(id:, **opts)
          @raw.finance_ledger_account_resources_id_get(id, opts)
        end

        # POST finance/ledger_account_resources/upsert
        def upsert(**opts)
          @raw.finance_ledger_account_resources_upsert_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.tax_rate — wraps the raw api.finance_tax_rate.
      class FinanceTaxRateResource
        def initialize(client)
          @raw = client.finance_tax_rate
        end

        # GET finance/tax_rates
        def list(**opts)
          @raw.finance_tax_rates_get(opts)
        end

        # GET finance/tax_rates/{id}
        def get(id:, **opts)
          @raw.finance_tax_rates_id_get(id, opts)
        end

        # POST finance/tax_rates
        def create(**opts)
          @raw.finance_tax_rates_post(opts)
        end

        # PUT finance/tax_rates/{id}
        def update(id:, **opts)
          @raw.finance_tax_rates_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.finance.tax_type — wraps the raw api.finance_tax_type.
      class FinanceTaxTypeResource
        def initialize(client)
          @raw = client.finance_tax_type
        end

        # GET finance/tax_types
        def list(**opts)
          @raw.finance_tax_types_get(opts)
        end

        # GET finance/tax_types/{id}
        def get(id:, **opts)
          @raw.finance_tax_types_id_get(id, opts)
        end

        # POST finance/tax_types
        def create(**opts)
          @raw.finance_tax_types_post(opts)
        end

        # PUT finance/tax_types/{id}
        def update(id:, **opts)
          @raw.finance_tax_types_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.holidays.company_holiday — wraps the raw api.holidays_company_holiday.
      class HolidaysCompanyHolidayResource
        def initialize(client)
          @raw = client.holidays_company_holiday
        end

        # GET holidays/company_holidays
        def list(**opts)
          @raw.holidays_company_holidays_get(opts)
        end

        # GET holidays/company_holidays/{id}
        def get(id:, **opts)
          @raw.holidays_company_holidays_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.integrations.sync_run_output — wraps the raw api.integrations_sync_run_output.
      class IntegrationsSyncRunOutputResource
        def initialize(client)
          @raw = client.integrations_sync_run_output
        end

        # POST integrations/sync_run_outputs
        def create(sync_run_id:, file:, **opts)
          @raw.integrations_sync_run_outputs_post(sync_run_id, file, opts)
        end
      end

      # api.integrations.syncable_item — wraps the raw api.integrations_syncable_item.
      class IntegrationsSyncableItemResource
        def initialize(client)
          @raw = client.integrations_syncable_item
        end

        # GET integrations/syncable_items
        def list(sync_run_id:, **opts)
          @raw.integrations_syncable_items_get(sync_run_id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(sync_run_id:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(sync_run_id: sync_run_id, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(sync_run_id:, **opts)
          paginate(sync_run_id: sync_run_id, **opts).to_a
        end
      end

      # api.integrations.syncable_sync_run — wraps the raw api.integrations_syncable_sync_run.
      class IntegrationsSyncableSyncRunResource
        def initialize(client)
          @raw = client.integrations_syncable_sync_run
        end

        # PUT integrations/syncable_sync_runs/{id}
        def update(id:, **opts)
          @raw.integrations_syncable_sync_runs_id_put(id, opts)
        end
      end

      # api.it_management.it_asset — wraps the raw api.it_management_it_asset.
      class ItManagementItAssetResource
        def initialize(client)
          @raw = client.it_management_it_asset
        end

        # GET it_management/it_assets
        def list(**opts)
          @raw.it_management_it_assets_get(opts)
        end

        # GET it_management/it_assets/{id}
        def get(id:, **opts)
          @raw.it_management_it_assets_id_get(id, opts)
        end

        # POST it_management/it_assets
        def create(**opts)
          @raw.it_management_it_assets_post(opts)
        end

        # PUT it_management/it_assets/{id}
        def update(id:, **opts)
          @raw.it_management_it_assets_id_put(id, opts)
        end

        # DELETE it_management/it_assets/{id}
        def delete(id:, **opts)
          @raw.it_management_it_assets_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.it_management.it_asset_model — wraps the raw api.it_management_it_asset_model.
      class ItManagementItAssetModelResource
        def initialize(client)
          @raw = client.it_management_it_asset_model
        end

        # GET it_management/it_asset_models
        def list(**opts)
          @raw.it_management_it_asset_models_get(opts)
        end

        # GET it_management/it_asset_models/{id}
        def get(id:, **opts)
          @raw.it_management_it_asset_models_id_get(id, opts)
        end

        # POST it_management/it_asset_models
        def create(**opts)
          @raw.it_management_it_asset_models_post(opts)
        end

        # PUT it_management/it_asset_models/{id}
        def update(id:, **opts)
          @raw.it_management_it_asset_models_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.job_catalog.level — wraps the raw api.job_catalog_level.
      class JobCatalogLevelResource
        def initialize(client)
          @raw = client.job_catalog_level
        end

        # GET job_catalog/levels
        def list(**opts)
          @raw.job_catalog_levels_get(opts)
        end

        # GET job_catalog/levels/{id}
        def get(id:, **opts)
          @raw.job_catalog_levels_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.job_catalog.node_attribute — wraps the raw api.job_catalog_node_attribute.
      class JobCatalogNodeAttributeResource
        def initialize(client)
          @raw = client.job_catalog_node_attribute
        end

        # GET job_catalog/node_attributes
        def list(node_uuid:, attribute_types:, **opts)
          @raw.job_catalog_node_attributes_get(node_uuid, attribute_types, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(node_uuid:, attribute_types:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(node_uuid: node_uuid, attribute_types: attribute_types, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(node_uuid:, attribute_types:, **opts)
          paginate(node_uuid: node_uuid, attribute_types: attribute_types, **opts).to_a
        end
      end

      # api.job_catalog.role — wraps the raw api.job_catalog_role.
      class JobCatalogRoleResource
        def initialize(client)
          @raw = client.job_catalog_role
        end

        # GET job_catalog/roles
        def list(**opts)
          @raw.job_catalog_roles_get(opts)
        end

        # GET job_catalog/roles/{id}
        def get(id:, **opts)
          @raw.job_catalog_roles_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.job_catalog.tree_node — wraps the raw api.job_catalog_tree_node.
      class JobCatalogTreeNodeResource
        def initialize(client)
          @raw = client.job_catalog_tree_node
        end

        # GET job_catalog/tree_nodes
        def list(node_type:, **opts)
          @raw.job_catalog_tree_nodes_get(node_type, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(node_type:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(node_type: node_type, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(node_type:, **opts)
          paginate(node_type: node_type, **opts).to_a
        end
      end

      # api.locations.location — wraps the raw api.locations_location.
      class LocationsLocationResource
        def initialize(client)
          @raw = client.locations_location
        end

        # GET locations/locations
        def list(**opts)
          @raw.locations_locations_get(opts)
        end

        # GET locations/locations/{id}
        def get(id:, **opts)
          @raw.locations_locations_id_get(id, opts)
        end

        # POST locations/locations
        def create(**opts)
          @raw.locations_locations_post(opts)
        end

        # PUT locations/locations/{id}
        def update(id:, **opts)
          @raw.locations_locations_id_put(id, opts)
        end

        # DELETE locations/locations/{id}
        def delete(id:, **opts)
          @raw.locations_locations_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.locations.work_area — wraps the raw api.locations_work_area.
      class LocationsWorkAreaResource
        def initialize(client)
          @raw = client.locations_work_area
        end

        # GET locations/work_areas
        def list(only_non_archived:, **opts)
          @raw.locations_work_areas_get(only_non_archived, opts)
        end

        # GET locations/work_areas/{id}
        def get(id:, **opts)
          @raw.locations_work_areas_id_get(id, opts)
        end

        # POST locations/work_areas
        def create(**opts)
          @raw.locations_work_areas_post(opts)
        end

        # PUT locations/work_areas/{id}
        def update(id:, **opts)
          @raw.locations_work_areas_id_put(id, opts)
        end

        # POST locations/work_areas/archive
        def archive(**opts)
          @raw.locations_work_areas_archive_post(opts)
        end

        # POST locations/work_areas/unarchive
        def unarchive(**opts)
          @raw.locations_work_areas_unarchive_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(only_non_archived:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(only_non_archived: only_non_archived, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(only_non_archived:, **opts)
          paginate(only_non_archived: only_non_archived, **opts).to_a
        end
      end

      # api.marketplace.installation — wraps the raw api.marketplace_installation.
      class MarketplaceInstallationResource
        def initialize(client)
          @raw = client.marketplace_installation
        end

        # POST marketplace/installations
        def create(**opts)
          @raw.marketplace_installations_post(opts)
        end
      end

      # api.marketplace.installation_setting — wraps the raw api.marketplace_installation_setting.
      class MarketplaceInstallationSettingResource
        def initialize(client)
          @raw = client.marketplace_installation_setting
        end

        # GET marketplace/installation_settings
        def list(company_id:, integration_id:, **opts)
          @raw.marketplace_installation_settings_get(company_id, integration_id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(company_id:, integration_id:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(company_id: company_id, integration_id: integration_id, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(company_id:, integration_id:, **opts)
          paginate(company_id: company_id, integration_id: integration_id, **opts).to_a
        end
      end

      # api.payroll.family_situation — wraps the raw api.payroll_family_situation.
      class PayrollFamilySituationResource
        def initialize(client)
          @raw = client.payroll_family_situation
        end

        # GET payroll/family_situations
        def list(**opts)
          @raw.payroll_family_situations_get(opts)
        end

        # POST payroll/family_situations
        def create(**opts)
          @raw.payroll_family_situations_post(opts)
        end

        # PUT payroll/family_situations/{id}
        def update(id:, **opts)
          @raw.payroll_family_situations_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.payroll.policy_period — wraps the raw api.payroll_policy_period.
      class PayrollPolicyPeriodResource
        def initialize(client)
          @raw = client.payroll_policy_period
        end

        # POST payroll/policy_periods/change_status
        def change_status(**opts)
          @raw.payroll_policy_periods_change_status_post(opts)
        end
      end

      # api.payroll.supplement — wraps the raw api.payroll_supplement.
      class PayrollSupplementResource
        def initialize(client)
          @raw = client.payroll_supplement
        end

        # GET payroll/supplements
        def list(policy_period_ids:, **opts)
          @raw.payroll_supplements_get(policy_period_ids, opts)
        end

        # GET payroll/supplements/{id}
        def get(id:, **opts)
          @raw.payroll_supplements_id_get(id, opts)
        end

        # POST payroll/supplements
        def create(**opts)
          @raw.payroll_supplements_post(opts)
        end

        # PUT payroll/supplements/{id}
        def update(id:, **opts)
          @raw.payroll_supplements_id_put(id, opts)
        end

        # DELETE payroll/supplements/{id}
        def delete(id:, **opts)
          @raw.payroll_supplements_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(policy_period_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(policy_period_ids: policy_period_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(policy_period_ids:, **opts)
          paginate(policy_period_ids: policy_period_ids, **opts).to_a
        end
      end

      # api.payroll_employees.identifier — wraps the raw api.payroll_employees_identifier.
      class PayrollEmployeesIdentifierResource
        def initialize(client)
          @raw = client.payroll_employees_identifier
        end

        # GET payroll_employees/identifiers
        def list(country:, **opts)
          @raw.payroll_employees_identifiers_get(country, opts)
        end

        # GET payroll_employees/identifiers/{id}
        def get(id:, **opts)
          @raw.payroll_employees_identifiers_id_get(id, opts)
        end

        # POST payroll_employees/identifiers
        def create(**opts)
          @raw.payroll_employees_identifiers_post(opts)
        end

        # PUT payroll_employees/identifiers/{id}
        def update(id:, **opts)
          @raw.payroll_employees_identifiers_id_put(id, opts)
        end

        # DELETE payroll_employees/identifiers/{id}
        def delete(id:, **opts)
          @raw.payroll_employees_identifiers_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(country:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(country: country, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(country:, **opts)
          paginate(country: country, **opts).to_a
        end
      end

      # api.payroll_integrations_base.code — wraps the raw api.payroll_integrations_base_code.
      class PayrollIntegrationsBaseCodeResource
        def initialize(client)
          @raw = client.payroll_integrations_base_code
        end

        # GET payroll_integrations_base/codes
        def list(integrations:, **opts)
          @raw.payroll_integrations_base_codes_get(integrations, opts)
        end

        # POST payroll_integrations_base/codes
        def create(**opts)
          @raw.payroll_integrations_base_codes_post(opts)
        end

        # PUT payroll_integrations_base/codes/{id}
        def update(id:, **opts)
          @raw.payroll_integrations_base_codes_id_put(id, opts)
        end

        # DELETE payroll_integrations_base/codes/{id}
        def delete(id:, **opts)
          @raw.payroll_integrations_base_codes_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(integrations:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(integrations: integrations, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(integrations:, **opts)
          paginate(integrations: integrations, **opts).to_a
        end
      end

      # api.performance.agreement — wraps the raw api.performance_agreement.
      class PerformanceAgreementResource
        def initialize(client)
          @raw = client.performance_agreement
        end

        # GET performance/agreements
        def list(**opts)
          @raw.performance_agreements_get(opts)
        end

        # GET performance/agreements/{id}
        def get(id:, **opts)
          @raw.performance_agreements_id_get(id, opts)
        end

        # POST performance/agreements/bulk_initiate
        def bulk_initiate(**opts)
          @raw.performance_agreements_bulk_initiate_post(opts)
        end

        # POST performance/agreements/initiate
        def initiate(**opts)
          @raw.performance_agreements_initiate_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.company_employee_score_scale — wraps the raw api.performance_company_employee_score_scale.
      class PerformanceCompanyEmployeeScoreScaleResource
        def initialize(client)
          @raw = client.performance_company_employee_score_scale
        end

        # GET performance/company_employee_score_scales
        def list(**opts)
          @raw.performance_company_employee_score_scales_get(opts)
        end

        # GET performance/company_employee_score_scales/{id}
        def get(id:, **opts)
          @raw.performance_company_employee_score_scales_id_get(id, opts)
        end

        # POST performance/company_employee_score_scales/set
        def set(**opts)
          @raw.performance_company_employee_score_scales_set_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.employee_score_scale — wraps the raw api.performance_employee_score_scale.
      class PerformanceEmployeeScoreScaleResource
        def initialize(client)
          @raw = client.performance_employee_score_scale
        end

        # GET performance/employee_score_scales
        def list(**opts)
          @raw.performance_employee_score_scales_get(opts)
        end

        # GET performance/employee_score_scales/{id}
        def get(id:, **opts)
          @raw.performance_employee_score_scales_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_evaluation — wraps the raw api.performance_review_evaluation.
      class PerformanceReviewEvaluationResource
        def initialize(client)
          @raw = client.performance_review_evaluation
        end

        # GET performance/review_evaluations
        def list(**opts)
          @raw.performance_review_evaluations_get(opts)
        end

        # GET performance/review_evaluations/{id}
        def get(id:, **opts)
          @raw.performance_review_evaluations_id_get(id, opts)
        end

        # POST performance/review_evaluations/replace_reviewer
        def replace_reviewer(**opts)
          @raw.performance_review_evaluations_replace_reviewer_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_evaluation_answer — wraps the raw api.performance_review_evaluation_answer.
      class PerformanceReviewEvaluationAnswerResource
        def initialize(client)
          @raw = client.performance_review_evaluation_answer
        end

        # GET performance/review_evaluation_answers
        def list(**opts)
          @raw.performance_review_evaluation_answers_get(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_evaluation_score — wraps the raw api.performance_review_evaluation_score.
      class PerformanceReviewEvaluationScoreResource
        def initialize(client)
          @raw = client.performance_review_evaluation_score
        end

        # GET performance/review_evaluation_scores
        def list(**opts)
          @raw.performance_review_evaluation_scores_get(opts)
        end

        # GET performance/review_evaluation_scores/{id}
        def get(id:, **opts)
          @raw.performance_review_evaluation_scores_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_owner — wraps the raw api.performance_review_owner.
      class PerformanceReviewOwnerResource
        def initialize(client)
          @raw = client.performance_review_owner
        end

        # GET performance/review_owners
        def list(**opts)
          @raw.performance_review_owners_get(opts)
        end

        # DELETE performance/review_owners/{id}
        def delete(id:, **opts)
          @raw.performance_review_owners_id_delete(id, opts)
        end

        # POST performance/review_owners/bulk_create
        def bulk_create(**opts)
          @raw.performance_review_owners_bulk_create_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_process — wraps the raw api.performance_review_process.
      class PerformanceReviewProcessResource
        def initialize(client)
          @raw = client.performance_review_process
        end

        # GET performance/review_processes
        def list(**opts)
          @raw.performance_review_processes_get(opts)
        end

        # GET performance/review_processes/{id}
        def get(id:, **opts)
          @raw.performance_review_processes_id_get(id, opts)
        end

        # POST performance/review_processes
        def create(**opts)
          @raw.performance_review_processes_post(opts)
        end

        # DELETE performance/review_processes/{id}
        def delete(id:, **opts)
          @raw.performance_review_processes_id_delete(id, opts)
        end

        # POST performance/review_processes/create_from_template
        def create_from_template(**opts)
          @raw.performance_review_processes_create_from_template_post(opts)
        end

        # POST performance/review_processes/duplicate
        def duplicate(**opts)
          @raw.performance_review_processes_duplicate_post(opts)
        end

        # POST performance/review_processes/remind_in_bulk
        def remind_in_bulk(**opts)
          @raw.performance_review_processes_remind_in_bulk_post(opts)
        end

        # POST performance/review_processes/remove_schedule
        def remove_schedule(**opts)
          @raw.performance_review_processes_remove_schedule_post(opts)
        end

        # POST performance/review_processes/reopen
        def reopen(**opts)
          @raw.performance_review_processes_reopen_post(opts)
        end

        # POST performance/review_processes/schedule
        def schedule(**opts)
          @raw.performance_review_processes_schedule_post(opts)
        end

        # POST performance/review_processes/start
        def start(**opts)
          @raw.performance_review_processes_start_post(opts)
        end

        # POST performance/review_processes/stop
        def stop(**opts)
          @raw.performance_review_processes_stop_post(opts)
        end

        # POST performance/review_processes/toggle_archive
        def toggle_archive(**opts)
          @raw.performance_review_processes_toggle_archive_post(opts)
        end

        # POST performance/review_processes/update_agreements_configuration
        def update_agreements_configuration(**opts)
          @raw.performance_review_processes_update_agreements_configuration_post(opts)
        end

        # POST performance/review_processes/update_basic_info
        def update_basic_info(**opts)
          @raw.performance_review_processes_update_basic_info_post(opts)
        end

        # POST performance/review_processes/update_competencies_assessments_configuration
        def update_competencies_assessments_configuration(**opts)
          @raw.performance_review_processes_update_competencies_assessments_configuration_post(opts)
        end

        # POST performance/review_processes/update_deadline
        def update_deadline(**opts)
          @raw.performance_review_processes_update_deadline_post(opts)
        end

        # POST performance/review_processes/update_employee_score_configuration
        def update_employee_score_configuration(**opts)
          @raw.performance_review_processes_update_employee_score_configuration_post(opts)
        end

        # POST performance/review_processes/update_reviewer_strategies
        def update_reviewer_strategies(**opts)
          @raw.performance_review_processes_update_reviewer_strategies_post(opts)
        end

        # POST performance/review_processes/update_schedule
        def update_schedule(**opts)
          @raw.performance_review_processes_update_schedule_post(opts)
        end

        # POST performance/review_processes/update_target_strategy
        def update_target_strategy(**opts)
          @raw.performance_review_processes_update_target_strategy_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_process_custom_template — wraps the raw api.performance_review_process_custom_template.
      class PerformanceReviewProcessCustomTemplateResource
        def initialize(client)
          @raw = client.performance_review_process_custom_template
        end

        # GET performance/review_process_custom_templates
        def list(**opts)
          @raw.performance_review_process_custom_templates_get(opts)
        end

        # GET performance/review_process_custom_templates/{id}
        def get(id:, **opts)
          @raw.performance_review_process_custom_templates_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_process_estimated_target — wraps the raw api.performance_review_process_estimated_target.
      class PerformanceReviewProcessEstimatedTargetResource
        def initialize(client)
          @raw = client.performance_review_process_estimated_target
        end

        # GET performance/review_process_estimated_targets
        def list(**opts)
          @raw.performance_review_process_estimated_targets_get(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_process_target — wraps the raw api.performance_review_process_target.
      class PerformanceReviewProcessTargetResource
        def initialize(client)
          @raw = client.performance_review_process_target
        end

        # GET performance/review_process_targets
        def list(**opts)
          @raw.performance_review_process_targets_get(opts)
        end

        # GET performance/review_process_targets/{id}
        def get(id:, **opts)
          @raw.performance_review_process_targets_id_get(id, opts)
        end

        # DELETE performance/review_process_targets/{id}
        def delete(id:, **opts)
          @raw.performance_review_process_targets_id_delete(id, opts)
        end

        # POST performance/review_process_targets/add_peers
        def add_peers(**opts)
          @raw.performance_review_process_targets_add_peers_post(opts)
        end

        # POST performance/review_process_targets/bulk_create
        def bulk_create(**opts)
          @raw.performance_review_process_targets_bulk_create_post(opts)
        end

        # POST performance/review_process_targets/remove_peer_evaluations
        def remove_peer_evaluations(**opts)
          @raw.performance_review_process_targets_remove_peer_evaluations_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_questionnaire_by_strategy — wraps the raw api.performance_review_questionnaire_by_strategy.
      class PerformanceReviewQuestionnaireByStrategyResource
        def initialize(client)
          @raw = client.performance_review_questionnaire_by_strategy
        end

        # GET performance/review_questionnaire_by_strategies
        def list(**opts)
          @raw.performance_review_questionnaire_by_strategies_get(opts)
        end

        # GET performance/review_questionnaire_by_strategies/{id}
        def get(id:, **opts)
          @raw.performance_review_questionnaire_by_strategies_id_get(id, opts)
        end

        # POST performance/review_questionnaire_by_strategies/update_default_rating_scale
        def update_default_rating_scale(**opts)
          @raw.performance_review_questionnaire_by_strategies_update_default_rating_scale_post(opts)
        end

        # POST performance/review_questionnaire_by_strategies/update_questionnaire_for_strategy
        def update_questionnaire_for_strategy(**opts)
          @raw.performance_review_questionnaire_by_strategies_update_questionnaire_for_strategy_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.review_visibility_setting — wraps the raw api.performance_review_visibility_setting.
      class PerformanceReviewVisibilitySettingResource
        def initialize(client)
          @raw = client.performance_review_visibility_setting
        end

        # GET performance/review_visibility_settings
        def list(**opts)
          @raw.performance_review_visibility_settings_get(opts)
        end

        # PUT performance/review_visibility_settings/{id}
        def update(id:, **opts)
          @raw.performance_review_visibility_settings_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.performance.target_manager — wraps the raw api.performance_target_manager.
      class PerformanceTargetManagerResource
        def initialize(client)
          @raw = client.performance_target_manager
        end

        # GET performance/target_managers
        def list(performance_review_process_ids:, **opts)
          @raw.performance_target_managers_get(performance_review_process_ids, opts)
        end

        # GET performance/target_managers/{id}
        def get(id:, **opts)
          @raw.performance_target_managers_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(performance_review_process_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(performance_review_process_ids: performance_review_process_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(performance_review_process_ids:, **opts)
          paginate(performance_review_process_ids: performance_review_process_ids, **opts).to_a
        end
      end

      # api.posts.comment — wraps the raw api.posts_comment.
      class PostsCommentResource
        def initialize(client)
          @raw = client.posts_comment
        end

        # GET posts/comments
        def list(post_ids:, **opts)
          @raw.posts_comments_get(post_ids, opts)
        end

        # GET posts/comments/{id}
        def get(id:, **opts)
          @raw.posts_comments_id_get(id, opts)
        end

        # POST posts/comments
        def create(**opts)
          @raw.posts_comments_post(opts)
        end

        # PUT posts/comments/{id}
        def update(id:, **opts)
          @raw.posts_comments_id_put(id, opts)
        end

        # DELETE posts/comments/{id}
        def delete(id:, **opts)
          @raw.posts_comments_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(post_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(post_ids: post_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(post_ids:, **opts)
          paginate(post_ids: post_ids, **opts).to_a
        end
      end

      # api.posts.group — wraps the raw api.posts_group.
      class PostsGroupResource
        def initialize(client)
          @raw = client.posts_group
        end

        # GET posts/groups
        def list(**opts)
          @raw.posts_groups_get(opts)
        end

        # GET posts/groups/{id}
        def get(id:, **opts)
          @raw.posts_groups_id_get(id, opts)
        end

        # POST posts/groups
        def create(**opts)
          @raw.posts_groups_post(opts)
        end

        # PUT posts/groups/{id}
        def update(id:, **opts)
          @raw.posts_groups_id_put(id, opts)
        end

        # DELETE posts/groups/{id}
        def delete(id:, **opts)
          @raw.posts_groups_id_delete(id, opts)
        end

        # POST posts/groups/archive
        def archive(**opts)
          @raw.posts_groups_archive_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.posts.post — wraps the raw api.posts_post.
      class PostsPostResource
        def initialize(client)
          @raw = client.posts_post
        end

        # GET posts/posts
        def list(**opts)
          @raw.posts_posts_get(opts)
        end

        # GET posts/posts/{id}
        def get(id:, **opts)
          @raw.posts_posts_id_get(id, opts)
        end

        # POST posts/posts
        def create(**opts)
          @raw.posts_posts_post(opts)
        end

        # PUT posts/posts/{id}
        def update(id:, **opts)
          @raw.posts_posts_id_put(id, opts)
        end

        # DELETE posts/posts/{id}
        def delete(id:, **opts)
          @raw.posts_posts_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.procurement.purchase_order — wraps the raw api.procurement_purchase_order.
      class ProcurementPurchaseOrderResource
        def initialize(client)
          @raw = client.procurement_purchase_order
        end

        # GET procurement/purchase_orders
        def list(**opts)
          @raw.procurement_purchase_orders_get(opts)
        end

        # GET procurement/purchase_orders/{id}
        def get(id:, **opts)
          @raw.procurement_purchase_orders_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.procurement.purchase_request — wraps the raw api.procurement_purchase_request.
      class ProcurementPurchaseRequestResource
        def initialize(client)
          @raw = client.procurement_purchase_request
        end

        # GET procurement/purchase_requests
        def list(**opts)
          @raw.procurement_purchase_requests_get(opts)
        end

        # GET procurement/purchase_requests/{id}
        def get(id:, **opts)
          @raw.procurement_purchase_requests_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.procurement.type — wraps the raw api.procurement_type.
      class ProcurementTypeResource
        def initialize(client)
          @raw = client.procurement_type
        end

        # GET procurement/types
        def list(**opts)
          @raw.procurement_types_get(opts)
        end

        # GET procurement/types/{id}
        def get(id:, **opts)
          @raw.procurement_types_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.budget_strategy — wraps the raw api.project_management_budget_strategy.
      class ProjectManagementBudgetStrategyResource
        def initialize(client)
          @raw = client.project_management_budget_strategy
        end

        # GET project_management/budget_strategies
        def list(**opts)
          @raw.project_management_budget_strategies_get(opts)
        end

        # GET project_management/budget_strategies/{id}
        def get(id:, **opts)
          @raw.project_management_budget_strategies_id_get(id, opts)
        end

        # POST project_management/budget_strategies
        def create(**opts)
          @raw.project_management_budget_strategies_post(opts)
        end

        # PUT project_management/budget_strategies/{id}
        def update(id:, **opts)
          @raw.project_management_budget_strategies_id_put(id, opts)
        end

        # DELETE project_management/budget_strategies/{id}
        def delete(id:, **opts)
          @raw.project_management_budget_strategies_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.expense_record — wraps the raw api.project_management_expense_record.
      class ProjectManagementExpenseRecordResource
        def initialize(client)
          @raw = client.project_management_expense_record
        end

        # GET project_management/expense_records
        def list(**opts)
          @raw.project_management_expense_records_get(opts)
        end

        # GET project_management/expense_records/{id}
        def get(id:, **opts)
          @raw.project_management_expense_records_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.exportable_expense — wraps the raw api.project_management_exportable_expense.
      class ProjectManagementExportableExpenseResource
        def initialize(client)
          @raw = client.project_management_exportable_expense
        end

        # GET project_management/exportable_expenses
        def list(start_date:, end_date:, project_ids:, **opts)
          @raw.project_management_exportable_expenses_get(start_date, end_date, project_ids, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(start_date:, end_date:, project_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(start_date: start_date, end_date: end_date, project_ids: project_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(start_date:, end_date:, project_ids:, **opts)
          paginate(start_date: start_date, end_date: end_date, project_ids: project_ids, **opts).to_a
        end
      end

      # api.project_management.imputable_project — wraps the raw api.project_management_imputable_project.
      class ProjectManagementImputableProjectResource
        def initialize(client)
          @raw = client.project_management_imputable_project
        end

        # GET project_management/imputable_projects
        def list(**opts)
          @raw.project_management_imputable_projects_get(opts)
        end

        # GET project_management/imputable_projects/{id}
        def get(id:, **opts)
          @raw.project_management_imputable_projects_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.planned_record — wraps the raw api.project_management_planned_record.
      class ProjectManagementPlannedRecordResource
        def initialize(client)
          @raw = client.project_management_planned_record
        end

        # GET project_management/planned_records
        def list(**opts)
          @raw.project_management_planned_records_get(opts)
        end

        # GET project_management/planned_records/{id}
        def get(id:, **opts)
          @raw.project_management_planned_records_id_get(id, opts)
        end

        # PUT project_management/planned_records/{id}
        def update(id:, **opts)
          @raw.project_management_planned_records_id_put(id, opts)
        end

        # DELETE project_management/planned_records/{id}
        def delete(id:, **opts)
          @raw.project_management_planned_records_id_delete(id, opts)
        end

        # POST project_management/planned_records/bulk_create
        def bulk_create(**opts)
          @raw.project_management_planned_records_bulk_create_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.project — wraps the raw api.project_management_project.
      class ProjectManagementProjectResource
        def initialize(client)
          @raw = client.project_management_project
        end

        # GET project_management/projects
        def list(include_inputed_minutes:, **opts)
          @raw.project_management_projects_get(include_inputed_minutes, opts)
        end

        # GET project_management/projects/{id}
        def get(id:, **opts)
          @raw.project_management_projects_id_get(id, opts)
        end

        # POST project_management/projects
        def create(**opts)
          @raw.project_management_projects_post(opts)
        end

        # PUT project_management/projects/{id}
        def update(id:, **opts)
          @raw.project_management_projects_id_put(id, opts)
        end

        # POST project_management/projects/activate
        def activate(**opts)
          @raw.project_management_projects_activate_post(opts)
        end

        # POST project_management/projects/change_assignment
        def change_assignment(**opts)
          @raw.project_management_projects_change_assignment_post(opts)
        end

        # POST project_management/projects/change_status
        def change_status(**opts)
          @raw.project_management_projects_change_status_post(opts)
        end

        # POST project_management/projects/close
        def close(**opts)
          @raw.project_management_projects_close_post(opts)
        end

        # POST project_management/projects/soft_delete
        def soft_delete(**opts)
          @raw.project_management_projects_soft_delete_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_inputed_minutes:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_inputed_minutes: include_inputed_minutes, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_inputed_minutes:, **opts)
          paginate(include_inputed_minutes: include_inputed_minutes, **opts).to_a
        end
      end

      # api.project_management.project_task — wraps the raw api.project_management_project_task.
      class ProjectManagementProjectTaskResource
        def initialize(client)
          @raw = client.project_management_project_task
        end

        # GET project_management/project_tasks
        def list(ids:, project_ids:, subproject_ids:, completed:, overdue:, search:, due_status:, client_ids:, **opts)
          @raw.project_management_project_tasks_get(ids, project_ids, subproject_ids, completed, overdue, search, due_status, client_ids, opts)
        end

        # GET project_management/project_tasks/{id}
        def get(id:, **opts)
          @raw.project_management_project_tasks_id_get(id, opts)
        end

        # POST project_management/project_tasks
        def create(name:, project_id:, status:, **opts)
          @raw.project_management_project_tasks_post(name, project_id, status, opts)
        end

        # PUT project_management/project_tasks/{id}
        def update(id:, id2:, name:, project_id:, **opts)
          @raw.project_management_project_tasks_id_put(id, id2, name, project_id, opts)
        end

        # POST project_management/project_tasks/bulk_destroy
        def bulk_destroy(**opts)
          @raw.project_management_project_tasks_bulk_destroy_post(opts)
        end

        # POST project_management/project_tasks/bulk_duplicate
        def bulk_duplicate(**opts)
          @raw.project_management_project_tasks_bulk_duplicate_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(ids:, project_ids:, subproject_ids:, completed:, overdue:, search:, due_status:, client_ids:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(ids: ids, project_ids: project_ids, subproject_ids: subproject_ids, completed: completed, overdue: overdue, search: search, due_status: due_status, client_ids: client_ids, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(ids:, project_ids:, subproject_ids:, completed:, overdue:, search:, due_status:, client_ids:, **opts)
          paginate(ids: ids, project_ids: project_ids, subproject_ids: subproject_ids, completed: completed, overdue: overdue, search: search, due_status: due_status, client_ids: client_ids, **opts).to_a
        end
      end

      # api.project_management.project_worker — wraps the raw api.project_management_project_worker.
      class ProjectManagementProjectWorkerResource
        def initialize(client)
          @raw = client.project_management_project_worker
        end

        # GET project_management/project_workers
        def list(**opts)
          @raw.project_management_project_workers_get(opts)
        end

        # GET project_management/project_workers/{id}
        def get(id:, **opts)
          @raw.project_management_project_workers_id_get(id, opts)
        end

        # POST project_management/project_workers
        def create(**opts)
          @raw.project_management_project_workers_post(opts)
        end

        # POST project_management/project_workers/bulk_assign
        def bulk_assign(**opts)
          @raw.project_management_project_workers_bulk_assign_post(opts)
        end

        # POST project_management/project_workers/bulk_create
        def bulk_create(**opts)
          @raw.project_management_project_workers_bulk_create_post(opts)
        end

        # POST project_management/project_workers/unassign
        def unassign(**opts)
          @raw.project_management_project_workers_unassign_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.subproject — wraps the raw api.project_management_subproject.
      class ProjectManagementSubprojectResource
        def initialize(client)
          @raw = client.project_management_subproject
        end

        # GET project_management/subprojects
        def list(**opts)
          @raw.project_management_subprojects_get(opts)
        end

        # GET project_management/subprojects/{id}
        def get(id:, **opts)
          @raw.project_management_subprojects_id_get(id, opts)
        end

        # POST project_management/subprojects
        def create(**opts)
          @raw.project_management_subprojects_post(opts)
        end

        # PUT project_management/subprojects/{id}
        def update(id:, **opts)
          @raw.project_management_subprojects_id_put(id, opts)
        end

        # DELETE project_management/subprojects/{id}
        def delete(id:, **opts)
          @raw.project_management_subprojects_id_delete(id, opts)
        end

        # POST project_management/subprojects/rename
        def rename(**opts)
          @raw.project_management_subprojects_rename_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.project_management.time_record — wraps the raw api.project_management_time_record.
      class ProjectManagementTimeRecordResource
        def initialize(client)
          @raw = client.project_management_time_record
        end

        # GET project_management/time_records
        def list(**opts)
          @raw.project_management_time_records_get(opts)
        end

        # GET project_management/time_records/{id}
        def get(id:, **opts)
          @raw.project_management_time_records_id_get(id, opts)
        end

        # POST project_management/time_records
        def create(**opts)
          @raw.project_management_time_records_post(opts)
        end

        # DELETE project_management/time_records/{id}
        def delete(id:, **opts)
          @raw.project_management_time_records_id_delete(id, opts)
        end

        # POST project_management/time_records/bulk_delete
        def bulk_delete(**opts)
          @raw.project_management_time_records_bulk_delete_post(opts)
        end

        # POST project_management/time_records/bulk_process
        def bulk_process(**opts)
          @raw.project_management_time_records_bulk_process_post(opts)
        end

        # POST project_management/time_records/update_project_worker
        def update_project_worker(**opts)
          @raw.project_management_time_records_update_project_worker_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.shift_management.shift — wraps the raw api.shift_management_shift.
      class ShiftManagementShiftResource
        def initialize(client)
          @raw = client.shift_management_shift
        end

        # GET shift_management/shifts
        def list(**opts)
          @raw.shift_management_shifts_get(opts)
        end

        # GET shift_management/shifts/{id}
        def get(id:, **opts)
          @raw.shift_management_shifts_id_get(id, opts)
        end

        # POST shift_management/shifts
        def create(**opts)
          @raw.shift_management_shifts_post(opts)
        end

        # DELETE shift_management/shifts/{id}
        def delete(id:, **opts)
          @raw.shift_management_shifts_id_delete(id, opts)
        end

        # POST shift_management/shifts/bulk_create
        def bulk_create(**opts)
          @raw.shift_management_shifts_bulk_create_post(opts)
        end

        # POST shift_management/shifts/bulk_delete
        def bulk_delete(**opts)
          @raw.shift_management_shifts_bulk_delete_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.tasks.task — wraps the raw api.tasks_task.
      class TasksTaskResource
        def initialize(client)
          @raw = client.tasks_task
        end

        # GET tasks/tasks
        def list(**opts)
          @raw.tasks_tasks_get(opts)
        end

        # GET tasks/tasks/{id}
        def get(id:, **opts)
          @raw.tasks_tasks_id_get(id, opts)
        end

        # POST tasks/tasks
        def create(**opts)
          @raw.tasks_tasks_post(opts)
        end

        # PUT tasks/tasks/{id}
        def update(id:, **opts)
          @raw.tasks_tasks_id_put(id, opts)
        end

        # DELETE tasks/tasks/{id}
        def delete(id:, **opts)
          @raw.tasks_tasks_id_delete(id, opts)
        end

        # POST tasks/tasks/bulk_create
        def bulk_create(**opts)
          @raw.tasks_tasks_bulk_create_post(opts)
        end

        # POST tasks/tasks/bulk_delete
        def bulk_delete(**opts)
          @raw.tasks_tasks_bulk_delete_post(opts)
        end

        # POST tasks/tasks/bulk_update
        def bulk_update(**opts)
          @raw.tasks_tasks_bulk_update_post(opts)
        end

        # POST tasks/tasks/copy
        def copy(**opts)
          @raw.tasks_tasks_copy_post(opts)
        end

        # POST tasks/tasks/create_comment
        def create_comment(**opts)
          @raw.tasks_tasks_create_comment_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.tasks.task_file — wraps the raw api.tasks_task_file.
      class TasksTaskFileResource
        def initialize(client)
          @raw = client.tasks_task_file
        end

        # GET tasks/task_files
        def list(task_id:, **opts)
          @raw.tasks_task_files_get(task_id, opts)
        end

        # GET tasks/task_files/{id}
        def get(id:, **opts)
          @raw.tasks_task_files_id_get(id, opts)
        end

        # POST tasks/task_files
        def create(task_id:, file:, **opts)
          @raw.tasks_task_files_post(task_id, file, opts)
        end

        # DELETE tasks/task_files/{id}
        def delete(id:, **opts)
          @raw.tasks_task_files_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(task_id:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(task_id: task_id, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(task_id:, **opts)
          paginate(task_id: task_id, **opts).to_a
        end
      end

      # api.teams.membership — wraps the raw api.teams_membership.
      class TeamsMembershipResource
        def initialize(client)
          @raw = client.teams_membership
        end

        # GET teams/memberships
        def list(**opts)
          @raw.teams_memberships_get(opts)
        end

        # GET teams/memberships/{id}
        def get(id:, **opts)
          @raw.teams_memberships_id_get(id, opts)
        end

        # POST teams/memberships
        def create(**opts)
          @raw.teams_memberships_post(opts)
        end

        # PUT teams/memberships/{id}
        def update(id:, **opts)
          @raw.teams_memberships_id_put(id, opts)
        end

        # DELETE teams/memberships/{id}
        def delete(id:, **opts)
          @raw.teams_memberships_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.teams.team — wraps the raw api.teams_team.
      class TeamsTeamResource
        def initialize(client)
          @raw = client.teams_team
        end

        # GET teams/teams
        def list(**opts)
          @raw.teams_teams_get(opts)
        end

        # GET teams/teams/{id}
        def get(id:, **opts)
          @raw.teams_teams_id_get(id, opts)
        end

        # POST teams/teams
        def create(**opts)
          @raw.teams_teams_post(opts)
        end

        # PUT teams/teams/{id}
        def update(id:, **opts)
          @raw.teams_teams_id_put(id, opts)
        end

        # DELETE teams/teams/{id}
        def delete(id:, **opts)
          @raw.teams_teams_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.time_planning.planned_break — wraps the raw api.time_planning_planned_break.
      class TimePlanningPlannedBreakResource
        def initialize(client)
          @raw = client.time_planning_planned_break
        end

        # GET time_planning/planned_breaks
        def list(ids:, paid:, default_shift_ids:, shift_ids:, day_configuration_ids:, shift_configuration_ids:, active_break_configuration:, **opts)
          @raw.time_planning_planned_breaks_get(ids, paid, default_shift_ids, shift_ids, day_configuration_ids, shift_configuration_ids, active_break_configuration, opts)
        end

        # GET time_planning/planned_breaks/{id}
        def get(id:, **opts)
          @raw.time_planning_planned_breaks_id_get(id, opts)
        end

        # POST time_planning/planned_breaks/bulk_create
        def bulk_create(**opts)
          @raw.time_planning_planned_breaks_bulk_create_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(ids:, paid:, default_shift_ids:, shift_ids:, day_configuration_ids:, shift_configuration_ids:, active_break_configuration:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(ids: ids, paid: paid, default_shift_ids: default_shift_ids, shift_ids: shift_ids, day_configuration_ids: day_configuration_ids, shift_configuration_ids: shift_configuration_ids, active_break_configuration: active_break_configuration, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(ids:, paid:, default_shift_ids:, shift_ids:, day_configuration_ids:, shift_configuration_ids:, active_break_configuration:, **opts)
          paginate(ids: ids, paid: paid, default_shift_ids: default_shift_ids, shift_ids: shift_ids, day_configuration_ids: day_configuration_ids, shift_configuration_ids: shift_configuration_ids, active_break_configuration: active_break_configuration, **opts).to_a
        end
      end

      # api.time_planning.planning_version — wraps the raw api.time_planning_planning_version.
      class TimePlanningPlanningVersionResource
        def initialize(client)
          @raw = client.time_planning_planning_version
        end

        # GET time_planning/planning_versions
        def list(only_active:, **opts)
          @raw.time_planning_planning_versions_get(only_active, opts)
        end

        # POST time_planning/planning_versions
        def create(**opts)
          @raw.time_planning_planning_versions_post(opts)
        end

        # PUT time_planning/planning_versions/{id}
        def update(id:, **opts)
          @raw.time_planning_planning_versions_id_put(id, opts)
        end

        # DELETE time_planning/planning_versions/{id}
        def delete(id:, **opts)
          @raw.time_planning_planning_versions_id_delete(id, opts)
        end

        # POST time_planning/planning_versions/bulk_create
        def bulk_create(**opts)
          @raw.time_planning_planning_versions_bulk_create_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(only_active:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(only_active: only_active, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(only_active:, **opts)
          paginate(only_active: only_active, **opts).to_a
        end
      end

      # api.time_settings.break_configuration — wraps the raw api.time_settings_break_configuration.
      class TimeSettingsBreakConfigurationResource
        def initialize(client)
          @raw = client.time_settings_break_configuration
        end

        # GET time_settings/break_configurations
        def list(active:, **opts)
          @raw.time_settings_break_configurations_get(active, opts)
        end

        # GET time_settings/break_configurations/{id}
        def get(id:, **opts)
          @raw.time_settings_break_configurations_id_get(id, opts)
        end

        # POST time_settings/break_configurations
        def create(**opts)
          @raw.time_settings_break_configurations_post(opts)
        end

        # PUT time_settings/break_configurations/{id}
        def update(id:, **opts)
          @raw.time_settings_break_configurations_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(active:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(active: active, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(active:, **opts)
          paginate(active: active, **opts).to_a
        end
      end

      # api.timeoff.allowance — wraps the raw api.timeoff_allowance.
      class TimeoffAllowanceResource
        def initialize(client)
          @raw = client.timeoff_allowance
        end

        # GET timeoff/allowances
        def list(**opts)
          @raw.timeoff_allowances_get(opts)
        end

        # GET timeoff/allowances/{id}
        def get(id:, **opts)
          @raw.timeoff_allowances_id_get(id, opts)
        end

        # POST timeoff/allowances
        def create(**opts)
          @raw.timeoff_allowances_post(opts)
        end

        # PUT timeoff/allowances/{id}
        def update(id:, **opts)
          @raw.timeoff_allowances_id_put(id, opts)
        end

        # DELETE timeoff/allowances/{id}
        def delete(id:, **opts)
          @raw.timeoff_allowances_id_delete(id, opts)
        end

        # POST timeoff/allowances/delete_with_alt_allowance
        def delete_with_alt_allowance(**opts)
          @raw.timeoff_allowances_delete_with_alt_allowance_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.allowance_incidence — wraps the raw api.timeoff_allowance_incidence.
      class TimeoffAllowanceIncidenceResource
        def initialize(client)
          @raw = client.timeoff_allowance_incidence
        end

        # GET timeoff/allowance_incidences
        def list(**opts)
          @raw.timeoff_allowance_incidences_get(opts)
        end

        # GET timeoff/allowance_incidences/{id}
        def get(id:, **opts)
          @raw.timeoff_allowance_incidences_id_get(id, opts)
        end

        # POST timeoff/allowance_incidences
        def create(**opts)
          @raw.timeoff_allowance_incidences_post(opts)
        end

        # PUT timeoff/allowance_incidences/{id}
        def update(id:, **opts)
          @raw.timeoff_allowance_incidences_id_put(id, opts)
        end

        # DELETE timeoff/allowance_incidences/{id}
        def delete(id:, **opts)
          @raw.timeoff_allowance_incidences_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.allowance_stat — wraps the raw api.timeoff_allowance_stat.
      class TimeoffAllowanceStatResource
        def initialize(client)
          @raw = client.timeoff_allowance_stat
        end

        # GET timeoff/allowance_stats
        def list(**opts)
          @raw.timeoff_allowance_stats_get(opts)
        end

        # GET timeoff/allowance_stats/{id}
        def get(id:, **opts)
          @raw.timeoff_allowance_stats_id_get(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.blocked_period — wraps the raw api.timeoff_blocked_period.
      class TimeoffBlockedPeriodResource
        def initialize(client)
          @raw = client.timeoff_blocked_period
        end

        # GET timeoff/blocked_periods
        def list(**opts)
          @raw.timeoff_blocked_periods_get(opts)
        end

        # GET timeoff/blocked_periods/{id}
        def get(id:, **opts)
          @raw.timeoff_blocked_periods_id_get(id, opts)
        end

        # POST timeoff/blocked_periods
        def create(**opts)
          @raw.timeoff_blocked_periods_post(opts)
        end

        # PUT timeoff/blocked_periods/{id}
        def update(id:, **opts)
          @raw.timeoff_blocked_periods_id_put(id, opts)
        end

        # DELETE timeoff/blocked_periods/{id}
        def delete(id:, **opts)
          @raw.timeoff_blocked_periods_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.leave — wraps the raw api.timeoff_leave.
      class TimeoffLeaveResource
        def initialize(client)
          @raw = client.timeoff_leave
        end

        # GET timeoff/leaves
        def list(include_deleted_leaves:, **opts)
          @raw.timeoff_leaves_get(include_deleted_leaves, opts)
        end

        # GET timeoff/leaves/{id}
        def get(id:, **opts)
          @raw.timeoff_leaves_id_get(id, opts)
        end

        # POST timeoff/leaves
        def create(**opts)
          @raw.timeoff_leaves_post(opts)
        end

        # PUT timeoff/leaves/{id}
        def update(id:, **opts)
          @raw.timeoff_leaves_id_put(id, opts)
        end

        # DELETE timeoff/leaves/{id}
        def delete(id:, **opts)
          @raw.timeoff_leaves_id_delete(id, opts)
        end

        # POST timeoff/leaves/approve
        def approve(**opts)
          @raw.timeoff_leaves_approve_post(opts)
        end

        # POST timeoff/leaves/approve_all
        def approve_all(**opts)
          @raw.timeoff_leaves_approve_all_post(opts)
        end

        # POST timeoff/leaves/reject
        def reject(**opts)
          @raw.timeoff_leaves_reject_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(include_deleted_leaves:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(include_deleted_leaves: include_deleted_leaves, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(include_deleted_leaves:, **opts)
          paginate(include_deleted_leaves: include_deleted_leaves, **opts).to_a
        end
      end

      # api.timeoff.leave_type — wraps the raw api.timeoff_leave_type.
      class TimeoffLeaveTypeResource
        def initialize(client)
          @raw = client.timeoff_leave_type
        end

        # GET timeoff/leave_types
        def list(**opts)
          @raw.timeoff_leave_types_get(opts)
        end

        # GET timeoff/leave_types/{id}
        def get(id:, **opts)
          @raw.timeoff_leave_types_id_get(id, opts)
        end

        # POST timeoff/leave_types
        def create(**opts)
          @raw.timeoff_leave_types_post(opts)
        end

        # PUT timeoff/leave_types/{id}
        def update(id:, **opts)
          @raw.timeoff_leave_types_id_put(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.policy — wraps the raw api.timeoff_policy.
      class TimeoffPolicyResource
        def initialize(client)
          @raw = client.timeoff_policy
        end

        # GET timeoff/policies
        def list(**opts)
          @raw.timeoff_policies_get(opts)
        end

        # GET timeoff/policies/{id}
        def get(id:, **opts)
          @raw.timeoff_policies_id_get(id, opts)
        end

        # POST timeoff/policies
        def create(**opts)
          @raw.timeoff_policies_post(opts)
        end

        # PUT timeoff/policies/{id}
        def update(id:, **opts)
          @raw.timeoff_policies_id_put(id, opts)
        end

        # DELETE timeoff/policies/{id}
        def delete(id:, **opts)
          @raw.timeoff_policies_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.policy_assignment — wraps the raw api.timeoff_policy_assignment.
      class TimeoffPolicyAssignmentResource
        def initialize(client)
          @raw = client.timeoff_policy_assignment
        end

        # GET timeoff/policy_assignments
        def list(**opts)
          @raw.timeoff_policy_assignments_get(opts)
        end

        # GET timeoff/policy_assignments/{id}
        def get(id:, **opts)
          @raw.timeoff_policy_assignments_id_get(id, opts)
        end

        # POST timeoff/policy_assignments
        def create(**opts)
          @raw.timeoff_policy_assignments_post(opts)
        end

        # PUT timeoff/policy_assignments/{id}
        def update(id:, **opts)
          @raw.timeoff_policy_assignments_id_put(id, opts)
        end

        # DELETE timeoff/policy_assignments/{id}
        def delete(id:, **opts)
          @raw.timeoff_policy_assignments_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.timeoff.policy_timeline — wraps the raw api.timeoff_policy_timeline.
      class TimeoffPolicyTimelineResource
        def initialize(client)
          @raw = client.timeoff_policy_timeline
        end

        # GET timeoff/policy_timelines
        def list(employee_id:, reference_date:, **opts)
          @raw.timeoff_policy_timelines_get(employee_id, reference_date, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(employee_id:, reference_date:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(employee_id: employee_id, reference_date: reference_date, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(employee_id:, reference_date:, **opts)
          paginate(employee_id: employee_id, reference_date: reference_date, **opts).to_a
        end
      end

      # api.trainings.category — wraps the raw api.trainings_category.
      class TrainingsCategoryResource
        def initialize(client)
          @raw = client.trainings_category
        end

        # GET trainings/categories
        def list(**opts)
          @raw.trainings_categories_get(opts)
        end

        # GET trainings/categories/{id}
        def get(id:, **opts)
          @raw.trainings_categories_id_get(id, opts)
        end

        # POST trainings/categories
        def create(**opts)
          @raw.trainings_categories_post(opts)
        end

        # DELETE trainings/categories/{id}
        def delete(id:, **opts)
          @raw.trainings_categories_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.trainings.session — wraps the raw api.trainings_session.
      class TrainingsSessionResource
        def initialize(client)
          @raw = client.trainings_session
        end

        # GET trainings/sessions
        def list(**opts)
          @raw.trainings_sessions_get(opts)
        end

        # GET trainings/sessions/{id}
        def get(id:, **opts)
          @raw.trainings_sessions_id_get(id, opts)
        end

        # POST trainings/sessions
        def create(**opts)
          @raw.trainings_sessions_post(opts)
        end

        # PUT trainings/sessions/{id}
        def update(id:, **opts)
          @raw.trainings_sessions_id_put(id, opts)
        end

        # DELETE trainings/sessions/{id}
        def delete(id:, **opts)
          @raw.trainings_sessions_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.trainings.session_access_membership — wraps the raw api.trainings_session_access_membership.
      class TrainingsSessionAccessMembershipResource
        def initialize(client)
          @raw = client.trainings_session_access_membership
        end

        # GET trainings/session_access_memberships
        def list(session_id:, **opts)
          @raw.trainings_session_access_memberships_get(session_id, opts)
        end

        # GET trainings/session_access_memberships/{id}
        def get(id:, **opts)
          @raw.trainings_session_access_memberships_id_get(id, opts)
        end

        # POST trainings/session_access_memberships/bulk_create
        def bulk_create(**opts)
          @raw.trainings_session_access_memberships_bulk_create_post(opts)
        end

        # POST trainings/session_access_memberships/bulk_destroy
        def bulk_destroy(**opts)
          @raw.trainings_session_access_memberships_bulk_destroy_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(session_id:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(session_id: session_id, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(session_id:, **opts)
          paginate(session_id: session_id, **opts).to_a
        end
      end

      # api.trainings.session_attendance — wraps the raw api.trainings_session_attendance.
      class TrainingsSessionAttendanceResource
        def initialize(client)
          @raw = client.trainings_session_attendance
        end

        # GET trainings/session_attendances
        def list(**opts)
          @raw.trainings_session_attendances_get(opts)
        end

        # GET trainings/session_attendances/{id}
        def get(id:, **opts)
          @raw.trainings_session_attendances_id_get(id, opts)
        end

        # POST trainings/session_attendances/bulk_update
        def bulk_update(**opts)
          @raw.trainings_session_attendances_bulk_update_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.trainings.training — wraps the raw api.trainings_training.
      class TrainingsTrainingResource
        def initialize(client)
          @raw = client.trainings_training
        end

        # GET trainings/trainings
        def list(**opts)
          @raw.trainings_trainings_get(opts)
        end

        # GET trainings/trainings/{id}
        def get(id:, **opts)
          @raw.trainings_trainings_id_get(id, opts)
        end

        # POST trainings/trainings
        def create(**opts)
          @raw.trainings_trainings_post(opts)
        end

        # PUT trainings/trainings/{id}
        def update(id:, **opts)
          @raw.trainings_trainings_id_put(id, opts)
        end

        # DELETE trainings/trainings/{id}
        def delete(id:, **opts)
          @raw.trainings_trainings_id_delete(id, opts)
        end

        # POST trainings/trainings/bulk_delete
        def bulk_delete(**opts)
          @raw.trainings_trainings_bulk_delete_post(opts)
        end

        # POST trainings/trainings/bulk_update_catalog
        def bulk_update_catalog(**opts)
          @raw.trainings_trainings_bulk_update_catalog_post(opts)
        end

        # POST trainings/trainings/update_status
        def update_status(**opts)
          @raw.trainings_trainings_update_status_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.trainings.training_class — wraps the raw api.trainings_training_class.
      class TrainingsTrainingClassResource
        def initialize(client)
          @raw = client.trainings_training_class
        end

        # GET trainings/training_classes
        def list(**opts)
          @raw.trainings_training_classes_get(opts)
        end

        # GET trainings/training_classes/{id}
        def get(id:, **opts)
          @raw.trainings_training_classes_id_get(id, opts)
        end

        # POST trainings/training_classes
        def create(**opts)
          @raw.trainings_training_classes_post(opts)
        end

        # PUT trainings/training_classes/{id}
        def update(id:, **opts)
          @raw.trainings_training_classes_id_put(id, opts)
        end

        # DELETE trainings/training_classes/{id}
        def delete(id:, **opts)
          @raw.trainings_training_classes_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.trainings.training_membership — wraps the raw api.trainings_training_membership.
      class TrainingsTrainingMembershipResource
        def initialize(client)
          @raw = client.trainings_training_membership
        end

        # GET trainings/training_memberships
        def list(due_date:, **opts)
          @raw.trainings_training_memberships_get(due_date, opts)
        end

        # GET trainings/training_memberships/{id}
        def get(id:, **opts)
          @raw.trainings_training_memberships_id_get(id, opts)
        end

        # PUT trainings/training_memberships/{id}
        def update(id:, **opts)
          @raw.trainings_training_memberships_id_put(id, opts)
        end

        # POST trainings/training_memberships/bulk_create
        def bulk_create(**opts)
          @raw.trainings_training_memberships_bulk_create_post(opts)
        end

        # POST trainings/training_memberships/bulk_destroy
        def bulk_destroy(**opts)
          @raw.trainings_training_memberships_bulk_destroy_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(due_date:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(due_date: due_date, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(due_date:, **opts)
          paginate(due_date: due_date, **opts).to_a
        end
      end

      # api.work_schedule.day_configuration — wraps the raw api.work_schedule_day_configuration.
      class WorkScheduleDayConfigurationResource
        def initialize(client)
          @raw = client.work_schedule_day_configuration
        end

        # GET work_schedule/day_configurations
        def list(**opts)
          @raw.work_schedule_day_configurations_get(opts)
        end

        # GET work_schedule/day_configurations/{id}
        def get(id:, **opts)
          @raw.work_schedule_day_configurations_id_get(id, opts)
        end

        # POST work_schedule/day_configurations/bulk_cud
        def bulk_cud(**opts)
          @raw.work_schedule_day_configurations_bulk_cud_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.work_schedule.overlap_period — wraps the raw api.work_schedule_overlap_period.
      class WorkScheduleOverlapPeriodResource
        def initialize(client)
          @raw = client.work_schedule_overlap_period
        end

        # GET work_schedule/overlap_periods
        def list(**opts)
          @raw.work_schedule_overlap_periods_get(opts)
        end

        # GET work_schedule/overlap_periods/{id}
        def get(id:, **opts)
          @raw.work_schedule_overlap_periods_id_get(id, opts)
        end

        # POST work_schedule/overlap_periods
        def create(**opts)
          @raw.work_schedule_overlap_periods_post(opts)
        end

        # PUT work_schedule/overlap_periods/{id}
        def update(id:, **opts)
          @raw.work_schedule_overlap_periods_id_put(id, opts)
        end

        # DELETE work_schedule/overlap_periods/{id}
        def delete(id:, **opts)
          @raw.work_schedule_overlap_periods_id_delete(id, opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(**opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(**opts)
          paginate(**opts).to_a
        end
      end

      # api.work_schedule.schedule — wraps the raw api.work_schedule_schedule.
      class WorkScheduleScheduleResource
        def initialize(client)
          @raw = client.work_schedule_schedule
        end

        # GET work_schedule/schedules
        def list(with_employee_ids:, with_periods:, **opts)
          @raw.work_schedule_schedules_get(with_employee_ids, with_periods, opts)
        end

        # GET work_schedule/schedules/{id}
        def get(id:, **opts)
          @raw.work_schedule_schedules_id_get(id, opts)
        end

        # POST work_schedule/schedules
        def create(**opts)
          @raw.work_schedule_schedules_post(opts)
        end

        # PUT work_schedule/schedules/{id}
        def update(id:, **opts)
          @raw.work_schedule_schedules_id_put(id, opts)
        end

        # POST work_schedule/schedules/toggle_archive
        def toggle_archive(**opts)
          @raw.work_schedule_schedules_toggle_archive_post(opts)
        end

        # Lazy Enumerator over every list page (see F::Api.paginate).
        def paginate(with_employee_ids:, with_periods:, limit: nil, max_items: nil, **opts)
          F::Api.paginate(limit: limit, max_items: max_items) do |page|
            list(with_employee_ids: with_employee_ids, with_periods: with_periods, **opts, query_params: (opts[:query_params] || {}).merge(page))
          end
        end

        # Fetches every list page into one Array.
        def all(with_employee_ids:, with_periods:, **opts)
          paginate(with_employee_ids: with_employee_ids, with_periods: with_periods, **opts).to_a
        end
      end

      # api.api_public.* — one accessor per resource.
      class ApiPublicNamespace
        def initialize(client)
          @client = client
        end

        def credential
          @credential ||= ApiPublicCredentialResource.new(@client)
        end

        def webhook_subscription
          @webhook_subscription ||= ApiPublicWebhookSubscriptionResource.new(@client)
        end
      end

      # api.approvals.* — one accessor per resource.
      class ApprovalsNamespace
        def initialize(client)
          @client = client
        end

        def materialized_approvals_flow
          @materialized_approvals_flow ||= ApprovalsMaterializedApprovalsFlowResource.new(@client)
        end
      end

      # api.ats.* — one accessor per resource.
      class AtsNamespace
        def initialize(client)
          @client = client
        end

        def answer
          @answer ||= AtsAnswerResource.new(@client)
        end

        def application
          @application ||= AtsApplicationResource.new(@client)
        end

        def application_phase
          @application_phase ||= AtsApplicationPhaseResource.new(@client)
        end

        def candidate
          @candidate ||= AtsCandidateResource.new(@client)
        end

        def candidate_source
          @candidate_source ||= AtsCandidateSourceResource.new(@client)
        end

        def evaluation_form
          @evaluation_form ||= AtsEvaluationFormResource.new(@client)
        end

        def feedback
          @feedback ||= AtsFeedbackResource.new(@client)
        end

        def hiring_stage
          @hiring_stage ||= AtsHiringStageResource.new(@client)
        end

        def job_posting
          @job_posting ||= AtsJobPostingResource.new(@client)
        end

        def message
          @message ||= AtsMessageResource.new(@client)
        end

        def question
          @question ||= AtsQuestionResource.new(@client)
        end

        def rejection_reason
          @rejection_reason ||= AtsRejectionReasonResource.new(@client)
        end
      end

      # api.attendance.* — one accessor per resource.
      class AttendanceNamespace
        def initialize(client)
          @client = client
        end

        def break_configuration
          @break_configuration ||= AttendanceBreakConfigurationResource.new(@client)
        end

        def edit_timesheet_request
          @edit_timesheet_request ||= AttendanceEditTimesheetRequestResource.new(@client)
        end

        def estimated_time
          @estimated_time ||= AttendanceEstimatedTimeResource.new(@client)
        end

        def open_shift
          @open_shift ||= AttendanceOpenShiftResource.new(@client)
        end

        def overtime_request
          @overtime_request ||= AttendanceOvertimeRequestResource.new(@client)
        end

        def review
          @review ||= AttendanceReviewResource.new(@client)
        end

        def shift
          @shift ||= AttendanceShiftResource.new(@client)
        end

        def worked_time
          @worked_time ||= AttendanceWorkedTimeResource.new(@client)
        end
      end

      # api.banking.* — one accessor per resource.
      class BankingNamespace
        def initialize(client)
          @client = client
        end

        def bank_account
          @bank_account ||= BankingBankAccountResource.new(@client)
        end

        def card_payment
          @card_payment ||= BankingCardPaymentResource.new(@client)
        end

        def transaction
          @transaction ||= BankingTransactionResource.new(@client)
        end
      end

      # api.bookkeepers_management.* — one accessor per resource.
      class BookkeepersManagementNamespace
        def initialize(client)
          @client = client
        end

        def incidence
          @incidence ||= BookkeepersManagementIncidenceResource.new(@client)
        end
      end

      # api.companies.* — one accessor per resource.
      class CompaniesNamespace
        def initialize(client)
          @client = client
        end

        def legal_entity
          @legal_entity ||= CompaniesLegalEntityResource.new(@client)
        end
      end

      # api.compensations.* — one accessor per resource.
      class CompensationsNamespace
        def initialize(client)
          @client = client
        end

        def concept
          @concept ||= CompensationsConceptResource.new(@client)
        end
      end

      # api.contracts.* — one accessor per resource.
      class ContractsNamespace
        def initialize(client)
          @client = client
        end

        def compensation
          @compensation ||= ContractsCompensationResource.new(@client)
        end

        def contract_template
          @contract_template ||= ContractsContractTemplateResource.new(@client)
        end

        def contract_version
          @contract_version ||= ContractsContractVersionResource.new(@client)
        end

        def contract_version_history
          @contract_version_history ||= ContractsContractVersionHistoryResource.new(@client)
        end

        def contract_version_meta_datum
          @contract_version_meta_datum ||= ContractsContractVersionMetaDatumResource.new(@client)
        end

        def french_contract_type
          @french_contract_type ||= ContractsFrenchContractTypeResource.new(@client)
        end

        def german_contract_type
          @german_contract_type ||= ContractsGermanContractTypeResource.new(@client)
        end

        def materialized_template
          @materialized_template ||= ContractsMaterializedTemplateResource.new(@client)
        end

        def portuguese_contract_type
          @portuguese_contract_type ||= ContractsPortugueseContractTypeResource.new(@client)
        end

        def reference_contract
          @reference_contract ||= ContractsReferenceContractResource.new(@client)
        end

        def spanish_contract_type
          @spanish_contract_type ||= ContractsSpanishContractTypeResource.new(@client)
        end

        def spanish_education_level
          @spanish_education_level ||= ContractsSpanishEducationLevelResource.new(@client)
        end

        def spanish_professional_category
          @spanish_professional_category ||= ContractsSpanishProfessionalCategoryResource.new(@client)
        end

        def spanish_working_day_type
          @spanish_working_day_type ||= ContractsSpanishWorkingDayTypeResource.new(@client)
        end

        def taxonomy
          @taxonomy ||= ContractsTaxonomyResource.new(@client)
        end
      end

      # api.custom_fields.* — one accessor per resource.
      class CustomFieldsNamespace
        def initialize(client)
          @client = client
        end

        def field
          @field ||= CustomFieldsFieldResource.new(@client)
        end

        def option
          @option ||= CustomFieldsOptionResource.new(@client)
        end

        def resource_field
          @resource_field ||= CustomFieldsResourceFieldResource.new(@client)
        end

        def value
          @value ||= CustomFieldsValueResource.new(@client)
        end
      end

      # api.custom_resources.* — one accessor per resource.
      class CustomResourcesNamespace
        def initialize(client)
          @client = client
        end

        def resource
          @resource ||= CustomResourcesResourceResource.new(@client)
        end

        def schema
          @schema ||= CustomResourcesSchemaResource.new(@client)
        end

        def value
          @value ||= CustomResourcesValueResource.new(@client)
        end
      end

      # api.documents.* — one accessor per resource.
      class DocumentsNamespace
        def initialize(client)
          @client = client
        end

        def document
          @document ||= DocumentsDocumentResource.new(@client)
        end

        def download_url
          @download_url ||= DocumentsDownloadUrlResource.new(@client)
        end

        def folder
          @folder ||= DocumentsFolderResource.new(@client)
        end
      end

      # api.employee_updates.* — one accessor per resource.
      class EmployeeUpdatesNamespace
        def initialize(client)
          @client = client
        end

        def absence
          @absence ||= EmployeeUpdatesAbsenceResource.new(@client)
        end

        def contract_change
          @contract_change ||= EmployeeUpdatesContractChangeResource.new(@client)
        end

        def new_hire
          @new_hire ||= EmployeeUpdatesNewHireResource.new(@client)
        end

        def personal_change
          @personal_change ||= EmployeeUpdatesPersonalChangeResource.new(@client)
        end

        def summary
          @summary ||= EmployeeUpdatesSummaryResource.new(@client)
        end

        def termination
          @termination ||= EmployeeUpdatesTerminationResource.new(@client)
        end
      end

      # api.employees.* — one accessor per resource.
      class EmployeesNamespace
        def initialize(client)
          @client = client
        end

        def employee
          @employee ||= EmployeesEmployeeResource.new(@client)
        end
      end

      # api.expenses.* — one accessor per resource.
      class ExpensesNamespace
        def initialize(client)
          @client = client
        end

        def expensable
          @expensable ||= ExpensesExpensableResource.new(@client)
        end

        def expense
          @expense ||= ExpensesExpenseResource.new(@client)
        end

        def mileage
          @mileage ||= ExpensesMileageResource.new(@client)
        end

        def per_diem
          @per_diem ||= ExpensesPerDiemResource.new(@client)
        end
      end

      # api.finance.* — one accessor per resource.
      class FinanceNamespace
        def initialize(client)
          @client = client
        end

        def account
          @account ||= FinanceAccountResource.new(@client)
        end

        def accounting_setting
          @accounting_setting ||= FinanceAccountingSettingResource.new(@client)
        end

        def budget_option
          @budget_option ||= FinanceBudgetOptionResource.new(@client)
        end

        def category
          @category ||= FinanceCategoryResource.new(@client)
        end

        def contact
          @contact ||= FinanceContactResource.new(@client)
        end

        def cost_center
          @cost_center ||= FinanceCostCenterResource.new(@client)
        end

        def cost_center_membership
          @cost_center_membership ||= FinanceCostCenterMembershipResource.new(@client)
        end

        def financial_document
          @financial_document ||= FinanceFinancialDocumentResource.new(@client)
        end

        def journal_entry
          @journal_entry ||= FinanceJournalEntryResource.new(@client)
        end

        def journal_line
          @journal_line ||= FinanceJournalLineResource.new(@client)
        end

        def ledger_account_resource
          @ledger_account_resource ||= FinanceLedgerAccountResourceResource.new(@client)
        end

        def tax_rate
          @tax_rate ||= FinanceTaxRateResource.new(@client)
        end

        def tax_type
          @tax_type ||= FinanceTaxTypeResource.new(@client)
        end
      end

      # api.holidays.* — one accessor per resource.
      class HolidaysNamespace
        def initialize(client)
          @client = client
        end

        def company_holiday
          @company_holiday ||= HolidaysCompanyHolidayResource.new(@client)
        end
      end

      # api.integrations.* — one accessor per resource.
      class IntegrationsNamespace
        def initialize(client)
          @client = client
        end

        def sync_run_output
          @sync_run_output ||= IntegrationsSyncRunOutputResource.new(@client)
        end

        def syncable_item
          @syncable_item ||= IntegrationsSyncableItemResource.new(@client)
        end

        def syncable_sync_run
          @syncable_sync_run ||= IntegrationsSyncableSyncRunResource.new(@client)
        end
      end

      # api.it_management.* — one accessor per resource.
      class ItManagementNamespace
        def initialize(client)
          @client = client
        end

        def it_asset
          @it_asset ||= ItManagementItAssetResource.new(@client)
        end

        def it_asset_model
          @it_asset_model ||= ItManagementItAssetModelResource.new(@client)
        end
      end

      # api.job_catalog.* — one accessor per resource.
      class JobCatalogNamespace
        def initialize(client)
          @client = client
        end

        def level
          @level ||= JobCatalogLevelResource.new(@client)
        end

        def node_attribute
          @node_attribute ||= JobCatalogNodeAttributeResource.new(@client)
        end

        def role
          @role ||= JobCatalogRoleResource.new(@client)
        end

        def tree_node
          @tree_node ||= JobCatalogTreeNodeResource.new(@client)
        end
      end

      # api.locations.* — one accessor per resource.
      class LocationsNamespace
        def initialize(client)
          @client = client
        end

        def location
          @location ||= LocationsLocationResource.new(@client)
        end

        def work_area
          @work_area ||= LocationsWorkAreaResource.new(@client)
        end
      end

      # api.marketplace.* — one accessor per resource.
      class MarketplaceNamespace
        def initialize(client)
          @client = client
        end

        def installation
          @installation ||= MarketplaceInstallationResource.new(@client)
        end

        def installation_setting
          @installation_setting ||= MarketplaceInstallationSettingResource.new(@client)
        end
      end

      # api.payroll.* — one accessor per resource.
      class PayrollNamespace
        def initialize(client)
          @client = client
        end

        def family_situation
          @family_situation ||= PayrollFamilySituationResource.new(@client)
        end

        def policy_period
          @policy_period ||= PayrollPolicyPeriodResource.new(@client)
        end

        def supplement
          @supplement ||= PayrollSupplementResource.new(@client)
        end
      end

      # api.payroll_employees.* — one accessor per resource.
      class PayrollEmployeesNamespace
        def initialize(client)
          @client = client
        end

        def identifier
          @identifier ||= PayrollEmployeesIdentifierResource.new(@client)
        end
      end

      # api.payroll_integrations_base.* — one accessor per resource.
      class PayrollIntegrationsBaseNamespace
        def initialize(client)
          @client = client
        end

        def code
          @code ||= PayrollIntegrationsBaseCodeResource.new(@client)
        end
      end

      # api.performance.* — one accessor per resource.
      class PerformanceNamespace
        def initialize(client)
          @client = client
        end

        def agreement
          @agreement ||= PerformanceAgreementResource.new(@client)
        end

        def company_employee_score_scale
          @company_employee_score_scale ||= PerformanceCompanyEmployeeScoreScaleResource.new(@client)
        end

        def employee_score_scale
          @employee_score_scale ||= PerformanceEmployeeScoreScaleResource.new(@client)
        end

        def review_evaluation
          @review_evaluation ||= PerformanceReviewEvaluationResource.new(@client)
        end

        def review_evaluation_answer
          @review_evaluation_answer ||= PerformanceReviewEvaluationAnswerResource.new(@client)
        end

        def review_evaluation_score
          @review_evaluation_score ||= PerformanceReviewEvaluationScoreResource.new(@client)
        end

        def review_owner
          @review_owner ||= PerformanceReviewOwnerResource.new(@client)
        end

        def review_process
          @review_process ||= PerformanceReviewProcessResource.new(@client)
        end

        def review_process_custom_template
          @review_process_custom_template ||= PerformanceReviewProcessCustomTemplateResource.new(@client)
        end

        def review_process_estimated_target
          @review_process_estimated_target ||= PerformanceReviewProcessEstimatedTargetResource.new(@client)
        end

        def review_process_target
          @review_process_target ||= PerformanceReviewProcessTargetResource.new(@client)
        end

        def review_questionnaire_by_strategy
          @review_questionnaire_by_strategy ||= PerformanceReviewQuestionnaireByStrategyResource.new(@client)
        end

        def review_visibility_setting
          @review_visibility_setting ||= PerformanceReviewVisibilitySettingResource.new(@client)
        end

        def target_manager
          @target_manager ||= PerformanceTargetManagerResource.new(@client)
        end
      end

      # api.posts.* — one accessor per resource.
      class PostsNamespace
        def initialize(client)
          @client = client
        end

        def comment
          @comment ||= PostsCommentResource.new(@client)
        end

        def group
          @group ||= PostsGroupResource.new(@client)
        end

        def post
          @post ||= PostsPostResource.new(@client)
        end
      end

      # api.procurement.* — one accessor per resource.
      class ProcurementNamespace
        def initialize(client)
          @client = client
        end

        def purchase_order
          @purchase_order ||= ProcurementPurchaseOrderResource.new(@client)
        end

        def purchase_request
          @purchase_request ||= ProcurementPurchaseRequestResource.new(@client)
        end

        def type
          @type ||= ProcurementTypeResource.new(@client)
        end
      end

      # api.project_management.* — one accessor per resource.
      class ProjectManagementNamespace
        def initialize(client)
          @client = client
        end

        def budget_strategy
          @budget_strategy ||= ProjectManagementBudgetStrategyResource.new(@client)
        end

        def expense_record
          @expense_record ||= ProjectManagementExpenseRecordResource.new(@client)
        end

        def exportable_expense
          @exportable_expense ||= ProjectManagementExportableExpenseResource.new(@client)
        end

        def imputable_project
          @imputable_project ||= ProjectManagementImputableProjectResource.new(@client)
        end

        def planned_record
          @planned_record ||= ProjectManagementPlannedRecordResource.new(@client)
        end

        def project
          @project ||= ProjectManagementProjectResource.new(@client)
        end

        def project_task
          @project_task ||= ProjectManagementProjectTaskResource.new(@client)
        end

        def project_worker
          @project_worker ||= ProjectManagementProjectWorkerResource.new(@client)
        end

        def subproject
          @subproject ||= ProjectManagementSubprojectResource.new(@client)
        end

        def time_record
          @time_record ||= ProjectManagementTimeRecordResource.new(@client)
        end
      end

      # api.shift_management.* — one accessor per resource.
      class ShiftManagementNamespace
        def initialize(client)
          @client = client
        end

        def shift
          @shift ||= ShiftManagementShiftResource.new(@client)
        end
      end

      # api.tasks.* — one accessor per resource.
      class TasksNamespace
        def initialize(client)
          @client = client
        end

        def task
          @task ||= TasksTaskResource.new(@client)
        end

        def task_file
          @task_file ||= TasksTaskFileResource.new(@client)
        end
      end

      # api.teams.* — one accessor per resource.
      class TeamsNamespace
        def initialize(client)
          @client = client
        end

        def membership
          @membership ||= TeamsMembershipResource.new(@client)
        end

        def team
          @team ||= TeamsTeamResource.new(@client)
        end
      end

      # api.time_planning.* — one accessor per resource.
      class TimePlanningNamespace
        def initialize(client)
          @client = client
        end

        def planned_break
          @planned_break ||= TimePlanningPlannedBreakResource.new(@client)
        end

        def planning_version
          @planning_version ||= TimePlanningPlanningVersionResource.new(@client)
        end
      end

      # api.time_settings.* — one accessor per resource.
      class TimeSettingsNamespace
        def initialize(client)
          @client = client
        end

        def break_configuration
          @break_configuration ||= TimeSettingsBreakConfigurationResource.new(@client)
        end
      end

      # api.timeoff.* — one accessor per resource.
      class TimeoffNamespace
        def initialize(client)
          @client = client
        end

        def allowance
          @allowance ||= TimeoffAllowanceResource.new(@client)
        end

        def allowance_incidence
          @allowance_incidence ||= TimeoffAllowanceIncidenceResource.new(@client)
        end

        def allowance_stat
          @allowance_stat ||= TimeoffAllowanceStatResource.new(@client)
        end

        def blocked_period
          @blocked_period ||= TimeoffBlockedPeriodResource.new(@client)
        end

        def leave
          @leave ||= TimeoffLeaveResource.new(@client)
        end

        def leave_type
          @leave_type ||= TimeoffLeaveTypeResource.new(@client)
        end

        def policy
          @policy ||= TimeoffPolicyResource.new(@client)
        end

        def policy_assignment
          @policy_assignment ||= TimeoffPolicyAssignmentResource.new(@client)
        end

        def policy_timeline
          @policy_timeline ||= TimeoffPolicyTimelineResource.new(@client)
        end
      end

      # api.trainings.* — one accessor per resource.
      class TrainingsNamespace
        def initialize(client)
          @client = client
        end

        def category
          @category ||= TrainingsCategoryResource.new(@client)
        end

        def session
          @session ||= TrainingsSessionResource.new(@client)
        end

        def session_access_membership
          @session_access_membership ||= TrainingsSessionAccessMembershipResource.new(@client)
        end

        def session_attendance
          @session_attendance ||= TrainingsSessionAttendanceResource.new(@client)
        end

        def training
          @training ||= TrainingsTrainingResource.new(@client)
        end

        def training_class
          @training_class ||= TrainingsTrainingClassResource.new(@client)
        end

        def training_membership
          @training_membership ||= TrainingsTrainingMembershipResource.new(@client)
        end
      end

      # api.work_schedule.* — one accessor per resource.
      class WorkScheduleNamespace
        def initialize(client)
          @client = client
        end

        def day_configuration
          @day_configuration ||= WorkScheduleDayConfigurationResource.new(@client)
        end

        def overlap_period
          @overlap_period ||= WorkScheduleOverlapPeriodResource.new(@client)
        end

        def schedule
          @schedule ||= WorkScheduleScheduleResource.new(@client)
        end
      end
    end

    # The ergonomic entry points: one accessor per domain namespace, added
    # to the facade without touching the handwritten api.rb (Ruby classes
    # are open, so this ADDS methods to the Client defined there).
    class Client
      def api_public
        @sdk_api_public ||= SDK::ApiPublicNamespace.new(self)
      end

      def approvals
        @sdk_approvals ||= SDK::ApprovalsNamespace.new(self)
      end

      def ats
        @sdk_ats ||= SDK::AtsNamespace.new(self)
      end

      def attendance
        @sdk_attendance ||= SDK::AttendanceNamespace.new(self)
      end

      def banking
        @sdk_banking ||= SDK::BankingNamespace.new(self)
      end

      def bookkeepers_management
        @sdk_bookkeepers_management ||= SDK::BookkeepersManagementNamespace.new(self)
      end

      def companies
        @sdk_companies ||= SDK::CompaniesNamespace.new(self)
      end

      def compensations
        @sdk_compensations ||= SDK::CompensationsNamespace.new(self)
      end

      def contracts
        @sdk_contracts ||= SDK::ContractsNamespace.new(self)
      end

      def custom_fields
        @sdk_custom_fields ||= SDK::CustomFieldsNamespace.new(self)
      end

      def custom_resources
        @sdk_custom_resources ||= SDK::CustomResourcesNamespace.new(self)
      end

      def documents
        @sdk_documents ||= SDK::DocumentsNamespace.new(self)
      end

      def employee_updates
        @sdk_employee_updates ||= SDK::EmployeeUpdatesNamespace.new(self)
      end

      def employees
        @sdk_employees ||= SDK::EmployeesNamespace.new(self)
      end

      def expenses
        @sdk_expenses ||= SDK::ExpensesNamespace.new(self)
      end

      def finance
        @sdk_finance ||= SDK::FinanceNamespace.new(self)
      end

      def holidays
        @sdk_holidays ||= SDK::HolidaysNamespace.new(self)
      end

      def integrations
        @sdk_integrations ||= SDK::IntegrationsNamespace.new(self)
      end

      def it_management
        @sdk_it_management ||= SDK::ItManagementNamespace.new(self)
      end

      def job_catalog
        @sdk_job_catalog ||= SDK::JobCatalogNamespace.new(self)
      end

      def locations
        @sdk_locations ||= SDK::LocationsNamespace.new(self)
      end

      def marketplace
        @sdk_marketplace ||= SDK::MarketplaceNamespace.new(self)
      end

      def payroll
        @sdk_payroll ||= SDK::PayrollNamespace.new(self)
      end

      def payroll_employees
        @sdk_payroll_employees ||= SDK::PayrollEmployeesNamespace.new(self)
      end

      def payroll_integrations_base
        @sdk_payroll_integrations_base ||= SDK::PayrollIntegrationsBaseNamespace.new(self)
      end

      def performance
        @sdk_performance ||= SDK::PerformanceNamespace.new(self)
      end

      def posts
        @sdk_posts ||= SDK::PostsNamespace.new(self)
      end

      def procurement
        @sdk_procurement ||= SDK::ProcurementNamespace.new(self)
      end

      def project_management
        @sdk_project_management ||= SDK::ProjectManagementNamespace.new(self)
      end

      def shift_management
        @sdk_shift_management ||= SDK::ShiftManagementNamespace.new(self)
      end

      def tasks
        @sdk_tasks ||= SDK::TasksNamespace.new(self)
      end

      def teams
        @sdk_teams ||= SDK::TeamsNamespace.new(self)
      end

      def time_planning
        @sdk_time_planning ||= SDK::TimePlanningNamespace.new(self)
      end

      def time_settings
        @sdk_time_settings ||= SDK::TimeSettingsNamespace.new(self)
      end

      def timeoff
        @sdk_timeoff ||= SDK::TimeoffNamespace.new(self)
      end

      def trainings
        @sdk_trainings ||= SDK::TrainingsNamespace.new(self)
      end

      def work_schedule
        @sdk_work_schedule ||= SDK::WorkScheduleNamespace.new(self)
      end
    end
  end
end
